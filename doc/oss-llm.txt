### OSS Callback Development Guide

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

A guide to help developers implement and debug the client-side upload and server-side callback handling.

```APIDOC
## Development Implementation Guide

Debugging upload callbacks involves two main parts: client-side upload and server-side callback processing. It is recommended to debug the client-side upload first, then the application server part, and finally perform end-to-end integration testing once both parts are debugged.
```

--------------------------------

### DeleteBucketQosInfo Example

Source: https://help.aliyun.com/zh/oss/developer-reference/api-delete-bucket-qos-info

An example of a DELETE request and the corresponding 200 OK response for the DeleteBucketQosInfo operation.

```http
DELETE /?qosInfo HTTP/1.1
Host: oss-example.oss-cn-hangzhou.aliyuncs.com  
Date: Fri, 27 Dec 2024 03:21:12 GMT
Authorization: OSS4-HMAC-SHA256 Credential=LTAI********************/20250417/cn-hangzhou/oss/aliyun_v4_request,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e
```

```http
HTTP/1.1 200 OK
Date: Fri, 27 Dec 2024 03:21:12 GMT
Content-Length: 0
Connection: keep-alive
Server: AliyunOSS
x-oss-request-id: 534****
```

--------------------------------

### Callback Body Example (x-www-form-urlencoded)

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

Example of callbackBody format for application/x-www-form-urlencoded content type. It uses OSS system parameters and custom variables.

```text
bucket=${bucket}&object=${object}&my_var_1=${x:my_var1}&my_var_2=${x:my_var2}
```

--------------------------------

### Python OSS Callback Server

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

Run the Python callback server demo directly. This requires the RSA dependency to be installed.

```bash
python callback_app_server.py
```

--------------------------------

### AppendObject Versioning Request and Response Examples

Source: https://help.aliyun.com/zh/oss/developer-reference/appendobject

HTTP request and response examples for AppendObject when versioning is enabled on the bucket.

```http
POST /example?append&position=0 HTTP/1.1 
Host: versioning-append.oss.aliyuncs.com 
Date: Tue, 09 Apr 2019 03:59:33 GMT
Content-Length: 3
Content-Type: application/octet-stream
Authorization: OSS4-HMAC-SHA256 Credential=LTAI********************/20250417/cn-hangzhou/oss/aliyun_v4_request,AdditionalHeaders=content-length,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e
```

```http
HTTP/1.1 200 OK
Date: Tue, 09 Apr 2019 03:59:33 GMT
ETag: "2776271A4A09D82CA518AC5C0000****"
Connection: keep-alive
Content-Length: 0  
Server: AliyunOSS
x-oss-version-id: CAEQGhiBgIC_k6aV5RgiIGI3YTY2ZmMzYWJlMzQ3YjM4YTljOTk5YjUyZGF****
x-oss-hash-crc64ecma: 3231342946509354535
x-oss-next-append-position: 47
x-oss-request-id: 5CAC18A5B7AEADE01700****
```

--------------------------------

### AppendObject Request and Response Examples

Source: https://help.aliyun.com/zh/oss/developer-reference/appendobject

Standard HTTP request and response examples for the AppendObject operation.

```http
POST /oss.jpg?append&position=0 HTTP/1.1 
Host: oss-example.oss.aliyuncs.com 
Cache-control: no-cache 
Expires: Wed, 08 Jul 2015 16:57:01 GMT 
x-oss-storage-class: Archive
Content-Disposition: attachment;filename=oss_download.jpg 
Date: Wed, 08 Jul 2015 06:57:01 GMT 
Content-Type: image/jpg 
Content-Length: 1717 
Authorization: OSS4-HMAC-SHA256 Credential=LTAI********************/20250417/cn-hangzhou/oss/aliyun_v4_request,AdditionalHeaders=content-disposition;content-length,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e  
[1717 bytes of object data]
```

```http
HTTP/1.1 200 OK
Date: Wed, 08 Jul 2015 06:57:01 GMT
ETag: "0F7230CAA4BE94CCBDC99C550000****"
Connection: keep-alive
Content-Length: 0  
Server: AliyunOSS
x-oss-hash-crc64ecma: 14741617095266562575
x-oss-next-append-position: 1717
x-oss-request-id: 559CC9BDC755F95A6448****
```

--------------------------------

### Callback Body Example (JSON)

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

Example of callbackBody format for application/json content type. It uses OSS system parameters and custom variables within a JSON structure.

```json
{"bucket":${bucket},"object":${object},"mimeType":${mimeType},"size":${size},"my_var1":${x:my_var1},"my_var2":${x:my_var2}}
```

--------------------------------

### Configure Acceleration Rules

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

Define acceleration rules in core-site.xml, combining prefix, size, and operation type configurations. This example enables acceleration for specific prefixes and includes head, tail, and size-based I/O range rules.

```xml
<property>
    <name>fs.oss.acc.rules</name>
    <value><![CDATA[
        <rules>
            <rule>
                <keyPrefixes>
                    <keyPrefix>tpcds/q9/</keyPrefix>
                </keyPrefixes>
                 <ioSizeRanges>
                      <ioSizeRange>
                          <ioType>HEAD</ioType>
                          <ioSize>2097152</ioSize>
                      </ioSizeRange>
                  
                      <ioSizeRange>
                          <ioType>TAIL</ioType>
                          <ioSize>2097152</ioSize>
                      </ioSizeRange>
                  
                      <ioSizeRange>
                          <ioType>SIZE</ioType>
                          <minIOSize>0</minIOSize>
                          <maxIOSize>52428800</maxIOSize>
                      </ioSizeRange>
                  </ioSizeRanges>
                <operations><operation>getObject</operation></operations>
            </rule>
        </rules>
    ]]></value>
</property>
```

--------------------------------

### Verify OSS Installation with Hadoop Commands

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

Use standard Hadoop file system commands to test basic connectivity and file operations.

```bash
# 列出 OSS 目录内容
hadoop fs -ls oss://my-data-lake-bucket/test/

# 上传小文件
hadoop fs -copyFromLocal /tmp/test.txt oss://my-data-lake-bucket/test/

# 删除文件
hadoop fs -rm oss://my-data-lake-bucket/test/test.txt
```

--------------------------------

### Successful Bucket Creation Response Example

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

Illustrates a successful HTTP response (200 OK) after creating an OSS bucket. A successful response typically has a Content-Length of 0 and may include a Location header indicating the created resource.

```http
HTTP/1.1 200 OK
x-oss-request-id: 534B371674E88A4D8906****
Date: Fri, 24 Feb 2017 03:15:40 GMT
Content-Length: 0
Connection: keep-alive
Server: AliyunOSS
Location: /oss-example
```

--------------------------------

### CleanRestoredObject Request Example

Source: https://help.aliyun.com/zh/oss/developer-reference/cleanrestoredobject

A concrete example of a CleanRestoredObject request including authentication headers.

```HTTP
POST /ObjectName?cleanRestoredObject HTTP/1.1
Host: examplebucket.oss-cn-hangzhou.aliyuncs.com
Date: Fri, 20 Sep 2024 08:32:21 GMT
Authorization: OSS4-HMAC-SHA256 Credential=LTAI********************/20250417/cn-hangzhou/oss/aliyun_v4_request,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e
```

--------------------------------

### Callback URL Encoding Example

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

Demonstrates how to URL-encode a callback URL to correctly handle special characters like Chinese characters.

```text
https://example.com/%E4%B8%AD%E6%96%87.php?key=value&%E4%B8%AD%E6%96%87%E5%90%8D%E7%A7%B0=%E4%B8%AD%E6%96%87%E5%80%BC
```

--------------------------------

### CloseMetaQuery Request Example

Source: https://help.aliyun.com/zh/oss/developer-reference/closemetaquery

An example of a CloseMetaQuery API request. This includes the Host, Date, and Authorization headers, which are crucial for authentication and routing.

```http
POST /?metaQuery&comp=delete HTTP/1.1
Host: oss-example.oss-cn-hangzhou.aliyuncs.com
Date: Thu, 17 Apr 2025 13:08:38 GMT
Authorization: OSS4-HMAC-SHA256 Credential=LTAI********************/20250417/cn-hangzhou/oss/aliyun_v4_request,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e
```

--------------------------------

### Create Bucket API Request Example

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

Demonstrates the structure of an HTTP PUT request to create an OSS bucket. Ensure to replace placeholders with your actual AccessKey, Endpoint, and other relevant details. The Authorization header uses the OSSv4-HMAC-SHA256 signature.

```http
PUT / HTTP/1.1
Host: oss-example.oss-cn-hangzhou.aliyuncs.com
Date: Thu, 17 Apr 2025 03:15:40 GMT
x-oss-acl: private
Authorization: OSS4-HMAC-SHA256 Credential=LTAI********************/20250417/cn-hangzhou/oss/aliyun_v4_request,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e
<?xml version="1.0" encoding="UTF-8"?>
<CreateBucketConfiguration>
    <StorageClass>Standard</StorageClass>
    <DataRedundancyType>LRS</DataRedundancyType>    
</CreateBucketConfiguration>
```

--------------------------------

### OSS POST Request to Application Server

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

This example shows the POST request sent by OSS to your application server upon successful file upload. Your server needs to be able to receive and process this request. The `Authorization` and `Content-MD5` headers are crucial for security.

```HTTP
POST /test HTTP/1.1
Host: your.callback.com
Connection: close
Authorization: GevnM3**********3j7AKluzWnubHSVWI4dY3VsIfUHYWnyw==
Content-MD5: iKU/O/JB***ZMd8Ftg==
Content-Type: application/x-www-form-urlencoded
Date: Tue, 07 May 2024 03:06:13 GMT
User-Agent: aliyun-oss-callback
x-oss-bucket: your_bucket
x-oss-pub-key-url: aHR0cHM6Ly9nb3NzcHVi**********vY2FsbGJeV92MS5wZW0=
x-oss-request-id: 66399AA50*****3334673EC2
x-oss-requester: 23313******948342006
x-oss-signature-version: 1.0
x-oss-tag: CALLBACK
bucket=your_bucket&object=your_object&uid=12345&order_id=67890
```

--------------------------------

### AbortMultipartUpload Example Request and Response

Source: https://help.aliyun.com/zh/oss/developer-reference/abortmultipartupload

Example of an HTTP request to abort a multipart upload and the corresponding successful response.

```plaintext
Delete /multipart.data?&uploadId=0004B9895DBBB6E****  HTTP/1.1
Host: oss-example.oss-cn-hangzhou.aliyuncs.com
Date: Wed, 22 Feb 2012 08:32:21 GMT
Authorization: OSS4-HMAC-SHA256 Credential=LTAI********************/20250417/cn-hangzhou/oss/aliyun_v4_request,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e
```

```plaintext
HTTP/1.1 204 
Server: AliyunOSS
Content-length: 0
Connection: keep-alive
x-oss-request-id: 059a22ba-6ba9-daed-5f3a-e48027df****
Date: Wed, 22 Feb 2012 08:32:21 GMT
x-oss-server-time: 86
```

--------------------------------

### AppendObject with Versioning Enabled

Source: https://help.aliyun.com/zh/oss/developer-reference/appendobject

Example demonstrating the AppendObject API call for a bucket with versioning enabled, showing the response headers including x-oss-version-id.

```APIDOC
## POST /example?append&position=0 HTTP/1.1

### Description
Appends data to an object in a versioned bucket. The response includes the object's version ID.

### Method
POST

### Endpoint
`/example?append&position=0`

### Request Headers
- **Host** (string) - Required - The endpoint of the OSS bucket.
- **Date** (string) - Required - The date and time the request was generated.
- **Content-Length** (integer) - Required - The size of the request body in bytes.
- **Content-Type** (string) - Optional - The MIME type of the object.
- **Authorization** (string) - Required - Authentication credentials for the request.

### Request Body
- **[object data]** (bytes) - Required - The data to be appended to the object.

### Request Example
```http
POST /example?append&position=0 HTTP/1.1 
Host: versioning-append.oss.aliyuncs.com 
Date: Tue, 09 Apr 2019 03:59:33 GMT
Content-Length: 3
Content-Type: application/octet-stream
Authorization: OSS4-HMAC-SHA256 Credential=LTAI********************/20250417/cn-hangzhou/oss/aliyun_v4_request,AdditionalHeaders=content-length,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e
```

### Response
#### Success Response (200 OK)
- **Date** (string) - The date and time the response was generated.
- **ETag** (string) - The ETag of the object.
- **Connection** (string) - Connection status.
- **Content-Length** (integer) - The size of the response body.
- **Server** (string) - The server software.
- **x-oss-version-id** (string) - The version ID of the object.
- **x-oss-hash-crc64ecma** (string) - The CRC64 hash of the appended data.
- **x-oss-next-append-position** (integer) - The next position for appending data.
- **x-oss-request-id** (string) - The request ID.

#### Response Example
```http
HTTP/1.1 200 OK
Date: Tue, 09 Apr 2019 03:59:33 GMT
ETag: "2776271A4A09D82CA518AC5C0000****"
Connection: keep-alive
Content-Length: 0  
Server: AliyunOSS
x-oss-version-id: CAEQGhiBgIC_k6aV5RgiIGI3YTY2ZmMzYWJlMzQ3YjM4YTljOTk5YjUyZGF****
x-oss-hash-crc64ecma: 3231342946509354535
x-oss-next-append-position: 47
x-oss-request-id: 5CAC18A5B7AEADE01700****
```
```

--------------------------------

### CloseMetaQuery Response Example

Source: https://help.aliyun.com/zh/oss/developer-reference/closemetaquery

A successful response example for the CloseMetaQuery API. A 200 OK status code indicates the request was received, though the operation is asynchronous.

```http
HTTP/1.1 200 OK
x-oss-request-id: 5C1B138A109F4E405B2D****
Date: Mon, 26 Jul 2021 13:08:38 GMT
Content-Length: 0
Connection: keep-alive
Server: AliyunOSS
x-oss-server-time: 178
```

--------------------------------

### Application Server Callback Response (JSON)

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

An application server should return an HTTP 200 OK response to OSS after processing a callback request. The response must include `Content-Length` and can be in JSON or XML format. This example shows a JSON response.

```HTTP
HTTP/1.0 200 OK
Server: BaseHTTP/0.3 Python/2.7.6
Date: Mon, 14 Sep 2015 12:37:27 GMT
Content-Type: application/json
Content-Length: 9
{"Status": "OK"}
```

--------------------------------

### Python HTTP Server for OSS Callback

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

A basic Python HTTP server implementation to handle OSS callback requests. Ensure you have the RSA dependency installed for signature verification.

```python
import http.server
import socketserver

class MyHTTPRequestHandler(http.server.SimpleHTTPRequestHandler):
    def do_POST(self):
        content_length = int(self.headers['Content-Length'])
        post_data = self.rfile.read(content_length)
        print(f"Received POST data: {post_data.decode()}")

        # Process the callback data here
        # For example, verify the signature, parse the body, etc.

        # Send a success response back to OSS
        self.send_response(200)
        self.send_header('Content-type', 'application/json')
        self.end_headers()
        self.wfile.write(b'{"Status":"OK"}')

    def send_header(self, keyword, value):
        # Override send_header to handle Content-Length correctly
        if keyword == 'Content-Length':
            value = str(len(resp_body))
        super().send_header(keyword, value)

    def end_headers(self):
        # Override end_headers to ensure headers are sent before writing body
        super().end_headers()
        self.wfile.write(resp_body.encode())

class MyHTTPServer(socketserver.TCPServer):
    def __init__(self, host, port):
        super().__init__((host, port), MyHTTPRequestHandler)

if __name__ == '__main__':
    # Placeholder for get_local_ip() function
    def get_local_ip():
        return '127.0.0.1' # Replace with actual logic to get local IP

    server_ip = get_local_ip()
    server_port = 23451
    server = MyHTTPServer(server_ip, server_port)
    print(f"Serving HTTP on {server_ip} port {server_port}...")
    server.serve_forever()
```

--------------------------------

### CleanRestoredObject Response Examples

Source: https://help.aliyun.com/zh/oss/developer-reference/cleanrestoredobject

Various response scenarios for the CleanRestoredObject operation, including error states and success.

```HTTP
HTTP/1.1 409 Conflict
Date: Fri, 20 Sep 2024 08:32:21 GMT
Content-Length: 556
Connection: keep-alive
Server: AliyunOSS
x-oss-request-id: 5374A2880232A65C2300****
<?xml version="1.0" encoding="UTF-8"?>
<Error>
  <Code>ArchiveRestoreFileStale</Code>
  <Message>The archive file or restore info stale</Message>
  <RequestId>5374A2880232A65C2300****</RequestId>
  <HostId>10.101.XX.XX</HostId>
</Error>
```

```HTTP
HTTP/1.1 409 Conflict
Date: Fri, 20 Sep 2024 08:32:21 GMT
Content-Length: 556
Connection: keep-alive
Server: AliyunOSS
x-oss-request-id: 5374A2880232A65C2300****
<?xml version="1.0" encoding="UTF-8"?>
<Error>
  <Code>ArchiveRestoreNotFinished</Code>
  <Message>The archive file's restore is not finished.</Message>
  <RequestId>5374A2880232A65C2300****</RequestId>
  <HostId>10.101.XX.XX</HostId>
</Error>
```

```HTTP
HTTP/1.1 200 Ok
Date: Fri, 20 Sep 2024 08:32:21 GMT
Content-Length: 0
Connection: keep-alive
Server: AliyunOSS
x-oss-request-id: 5374A2880232A65C2300****
```

--------------------------------

### Delete Unlocked Compliance Retention Policy Response Example

Source: https://help.aliyun.com/zh/oss/developer-reference/abortbucketworm

This is an example of a successful HTTP 204 No Content response when aborting a bucket worm (compliance retention policy). The response indicates the operation was successful.

```HTTP
HTTP/1.1 204 NoContent
Server: AliyunOSS
Date: Tue, 22 Aug 2023 09:10:20 GMT
Content-Length: 0
Connection: keep-alive
x-oss-request-id: 5C06A3B67B8B5A3DA422299D
x-oss-server-time: 130
```

--------------------------------

### Bucket QoS Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for setting, getting, and deleting QoS information for individual Buckets within a resource pool.

```APIDOC
## PutBucketQoSInfo

### Description
Sets the flow control for a Bucket within a resource pool.

### Method
POST

### Endpoint
/PutBucketQoSInfo

## GetBucketQoSInfo

### Description
Retrieves the flow control configuration for a specific Bucket within a resource pool.

### Method
GET

### Endpoint
/GetBucketQoSInfo

## DeleteBucketQosInfo

### Description
Deletes the flow control configuration for a specified Bucket within a resource pool.

### Method
DELETE

### Endpoint
/DeleteBucketQosInfo
```

--------------------------------

### Configure Connection Pool Size for OSS Client

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Set the `max_connections` parameter in `http_client` to configure the connection pool size. The default is 20, and this example sets it to 100.

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

# OSS Python SDK V2 连接池配置示例

import alibabacloud_oss_v2 as oss

def main():

    # 从环境变量中加载访问凭证（需要设置OSS_ACCESS_KEY_ID和OSS_ACCESS_KEY_SECRET）
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载SDK的默认配置，并设置凭证提供者
    config = oss.config.load_default()
    config.credentials_provider = credentials_provider

    # 设置OSS服务地域
    config.region = "<region-id>"

    # ============ 自定义HTTPClient配置 ============ 
    # 创建HTTP客户端配置参数字典
    http_client_config = {}
    
    # 设置连接池大小为100，默认为20
    http_client_config["max_connections"] = 100
    
    # 使用自定义配置创建HTTP客户端
    http_client = oss.transport.RequestsHttpClient(**http_client_config)
    
    # 将自定义的HTTP客户端设置到配置对象中
    config.http_client = http_client
    # ============================================ 
    
    # 初始化OSS客户端实例
    client = oss.Client(config)

    # 配置Bucket和文件信息
    bucket = "example-bucket"             # Bucket名称
    key = "dest.jpg"                      # OSS中的文件路径
    file_path = "dest.jpg"                # 本地保存路径

    # 将OSS中的文件下载到本地指定路径
    client.get_object_to_file(oss.GetObjectRequest(bucket, key), file_path)
    print(f"文件下载完成: {key} -> {file_path}")
    print(f"连接池大小已设置为: {http_client_config['max_connections']}")

if __name__ == "__main__":
    # 程序入口点
    try:
        main()
    except Exception as e:
        print(f"运行过程中发生错误: {e}")
        raise

```

--------------------------------

### AppendObject API Request and Response

Source: https://help.aliyun.com/zh/oss/developer-reference/appendobject

This section provides an example of an AppendObject request and its corresponding success response, including relevant headers like x-oss-hash-crc64ecma and x-oss-next-append-position.

```APIDOC
## POST /oss.jpg?append&position=0 HTTP/1.1

### Description
Appends data to an existing object or creates a new appendable object.

### Method
POST

### Endpoint
`/oss.jpg?append&position=0`

### Request Headers
- **Host** (string) - Required - The endpoint of the OSS bucket.
- **Cache-control** (string) - Optional - Caching directives.
- **Expires** (string) - Optional - Expiration date for the cache.
- **x-oss-storage-class** (string) - Optional - Storage class for the object.
- **Content-Disposition** (string) - Optional - Specifies how to present the file.
- **Date** (string) - Required - The date and time the request was generated.
- **Content-Type** (string) - Optional - The MIME type of the object.
- **Content-Length** (integer) - Required - The size of the request body in bytes.
- **Authorization** (string) - Required - Authentication credentials for the request.

### Request Body
- **[object data]** (bytes) - Required - The data to be appended to the object.

### Request Example
```http
POST /oss.jpg?append&position=0 HTTP/1.1 
Host: oss-example.oss.aliyuncs.com 
Cache-control: no-cache 
Expires: Wed, 08 Jul 2015 16:57:01 GMT 
x-oss-storage-class: Archive
Content-Disposition: attachment;filename=oss_download.jpg 
Date: Wed, 08 Jul 2015 06:57:01 GMT 
Content-Type: image/jpg 
Content-Length: 1717 
Authorization: OSS4-HMAC-SHA256 Credential=LTAI********************/20250417/cn-hangzhou/oss/aliyun_v4_request,AdditionalHeaders=content-disposition;content-length,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e  
[1717 bytes of object data]
```

### Response
#### Success Response (200 OK)
- **Date** (string) - The date and time the response was generated.
- **ETag** (string) - The ETag of the object.
- **Connection** (string) - Connection status.
- **Content-Length** (integer) - The size of the response body.
- **Server** (string) - The server software.
- **x-oss-hash-crc64ecma** (string) - The CRC64 hash of the appended data.
- **x-oss-next-append-position** (integer) - The next position for appending data.
- **x-oss-request-id** (string) - The request ID.

#### Response Example
```http
HTTP/1.1 200 OK
Date: Wed, 08 Jul 2015 06:57:01 GMT
ETag: "0F7230CAA4BE94CCBDC99C550000****"
Connection: keep-alive
Content-Length: 0  
Server: AliyunOSS
x-oss-hash-crc64ecma: 14741617095266562575
x-oss-next-append-position: 1717
x-oss-request-id: 559CC9BDC755F95A6448****
```
```

--------------------------------

### Delete Unlocked Compliance Retention Policy Request Example

Source: https://help.aliyun.com/zh/oss/developer-reference/abortbucketworm

This is an example of an HTTP DELETE request to abort a bucket worm (compliance retention policy). Ensure the Host header correctly specifies your bucket name and region.

```HTTP
DELETE /?worm HTTP/1.1
Date: Mon, 3 Aug 2020 03:15:40 GMT
Host: BucketName.oss.aliyuncs.com
Authorization: OSS4-HMAC-SHA256 Credential=LTAI********************/20250417/cn-hangzhou/oss/aliyun_v4_request,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e
```

--------------------------------

### Node.js OSS Callback Server

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

Run the Node.js callback server demo by executing the example.js file.

```bash
node example.js
```

--------------------------------

### 使用静态凭证初始化OSS客户端

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

通过StaticCredentialsProvider显式设置访问密钥，仅建议用于测试。

```python
import alibabacloud_oss_v2 as oss

def main():
    # 创建静态凭证提供者，显式设置访问密钥和密钥密码，请替换为您的RAM用户的AccessKey ID和AccessKey Secret
    credentials_provider = oss.credentials.StaticCredentialsProvider(
        access_key_id="RAM AccessKey ID",
        access_key_secret="RAM AccessKey Secret"
    )

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作...


# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### Initialize OSS Client with RAM User Credentials

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Use this method when running applications outside of Alibaba Cloud compute services and you have RAM user access keys. Ensure environment variables `ALIBABA_CLOUD_ACCESS_KEY_ID` and `ALIBABA_CLOUD_ACCESS_KEY_SECRET` are set.

```python
import os
from alibabacloud_credentials.client import Client
from alibabacloud_credentials.models import Config
import alibabacloud_oss_v2 as oss

def main():
    config = Config(
        # 从环境变量中获取RAM用户的访问密钥（AccessKey ID和AccessKey Secret）
        access_key_id=os.getenv('ALIBABA_CLOUD_ACCESS_KEY_ID'),
        access_key_secret=os.getenv('ALIBABA_CLOUD_ACCESS_KEY_SECRET'),
        type='ram_role_arn',
        # 要扮演的RAM角色ARN，示例值：acs:ram::123456789012****:role/adminrole，可以通过环境变量ALIBABA_CLOUD_ROLE_ARN设置RoleArn
        role_arn='<RoleArn>',
        # 角色会话名称，可以通过环境变量ALIBABA_CLOUD_ROLE_SESSION_NAME设置RoleSessionName
        role_session_name='<RoleSessionName>',
        # 设置更小的权限策略，非必填。示例值：{"Statement": [{"Action": ["*"],"Effect": "Allow","Resource": ["*"]}],"Version":"1"}
        policy='<Policy>',
        # 设置角色会话有效期，单位为秒，默认值为3600秒（1小时），非必填
        role_session_expiration=3600
    )

    cred_client = Client(config)

    def get_credentials_wrapper():
        cred = cred_client.get_credential()
        return oss.credentials.Credentials(access_key_id=cred.access_key_id, access_key_secret=cred.access_key_secret, security_token=cred.security_token)

    # 创建凭证提供者，用于动态加载凭证
    credentials_provider = oss.credentials.CredentialsProviderFunc(func=get_credentials_wrapper)

    # 加载OSS SDK的默认配置
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    # 创建OSS客户端实例
    client = oss.Client(cfg)

    # 使用client进行后续操作... 


# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数

```

--------------------------------

### Java OSS Callback Server

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

Download and run the Java callback server demo. Specify the port when running the JAR file.

```bash
java -jar oss-callback-server-demo.jar 9000
```

--------------------------------

### 配置 Linux 环境变量

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

在 Linux 环境下设置并验证 OSS 访问凭证。

```bash
echo "export OSS_ACCESS_KEY_ID='YOUR_ACCESS_KEY_ID'" >> ~/.bashrc
echo "export OSS_ACCESS_KEY_SECRET='YOUR_ACCESS_KEY_SECRET'" >> ~/.bashrc
```

```bash
source ~/.bashrc
```

```bash
echo $OSS_ACCESS_KEY_ID
echo $OSS_ACCESS_KEY_SECRET
```

--------------------------------

### Configure core-site.xml for OSS

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

Add these properties to $HADOOP_HOME/etc/hadoop/core-site.xml to enable OSS access, optimize connections, and configure multipart uploads and prefetch settings.

```xml
<configuration>
    <!-- OSS 访问凭证 -->
    <property>
        <name>fs.oss.accessKeyId</name>
        <value>YourAccessKeyId</value>
    </property>
    <property>
        <name>fs.oss.accessKeySecret</name>
        <value>YourAccessKeySecret</value>
    </property>

    <!-- OSS 内网 Endpoint（强烈建议使用内网，降低延迟与流量费用） -->
    <property>
        <name>fs.oss.endpoint</name>
        <value>oss-cn-hangzhou-internal.aliyuncs.com</value>
    </property>

    <!-- 连接参数优化 -->
    <property>
        <name>fs.oss.connection.maximum</name>
        <value>64</value>
    </property>
    <property>
        <name>fs.oss.connection.timeout</name>
        <value>200000</value>
    </property>
    <property>
        <name>fs.oss.attempts.maximum</name>
        <value>10</value>
    </property>

    <!-- 分片上传设置（大于 20MB 启用 Multipart Upload） -->
    <property>
        <name>fs.oss.multipart.upload.threshold</name>
        <value>20971520</value> <!-- 20MB -->
    </property>
    <property>
        <name>fs.oss.multipart.upload.size</name>
        <value>104857600</value> <!-- 100MB per part -->
    </property>

    <!-- Prefetch 加速大文件顺序读 -->
    <property>
        <name>fs.oss.prefetch.version</name>
        <value>v2</value>
    </property>
    <property>
        <name>fs.oss.prefetch.block.size</name>
        <value>131072</value> <!-- 128KB -->
    </property>
    <property>
        <name>fs.oss.prefetch.block.count</name>
        <value>8</value>
    </property>
    <property>
        <name>fs.oss.prefetch.io.threshold</name>
        <value>2097152</value> <!-- 2MB -->
    </property>

    <!-- Buffer 目录（建议使用 SSD） -->
    <property>
        <name>fs.oss.buffer.dir</name>
        <value>/data/oss-buffer,/mnt/disk2/oss-buffer</value>
    </property>
</configuration>
```

--------------------------------

### 使用环境变量初始化OSS客户端

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

通过EnvironmentVariableCredentialsProvider从环境变量加载凭证。

```python
import alibabacloud_oss_v2 as oss

def main():
    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作...

# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### 通过 Header 传递回调参数

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

在 HTTP 请求头中设置 x-oss-callback 和 x-oss-callback-var 字段。

```http
PUT /your_object HTTP/1.1
Host: callback-test.oss-test.aliyun-inc.com
Accept-Encoding: identity
Content-Length: 5
x-oss-callback-var: eyJ4OnVpZCI6ICIxMjM0NSIsICJ4Om9yZGVyX2lkIjogIjY3ODkwIn0=
User-Agent: aliyun-sdk-python/0.4.0 (Linux/2.6.32-220.23.2.ali1089.el5.x86_64/x86_64;2.5.4)
x-oss-callback: 
Host: callback-test.oss-test.aliyun-inc.com
Expect: 100-Continue
Date: Wed, 26 Apr 2023 03:46:17 GMT
Content-Type: text/plain
Authorization: OSS qn6q**************:77Dv****************
Test
```

--------------------------------

### 通过环境变量加载 OSS 凭证

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

使用 EnvironmentVariableCredentialsProvider 从环境变量中读取 STS 凭证并初始化 OSS 客户端。

```python
import alibabacloud_oss_v2 as oss

def main():
    # 从环境变量中加载访问OSS所需的认证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作...


# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### 配置 Windows 环境变量

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

在 Windows CMD 或 PowerShell 中设置并验证 OSS 访问凭证。

```bash
setx OSS_ACCESS_KEY_ID "YOUR_ACCESS_KEY_ID"
setx OSS_ACCESS_KEY_SECRET "YOUR_ACCESS_KEY_SECRET"
```

```bash
echo %OSS_ACCESS_KEY_ID%
echo %OSS_ACCESS_KEY_SECRET%
```

```powershell
[Environment]::SetEnvironmentVariable("OSS_ACCESS_KEY_ID", "YOUR_ACCESS_KEY_ID", [EnvironmentVariableTarget]::User)
[Environment]::SetEnvironmentVariable("OSS_ACCESS_KEY_SECRET", "YOUR_ACCESS_KEY_SECRET", [EnvironmentVariableTarget]::User)
```

```powershell
[Environment]::GetEnvironmentVariable("OSS_ACCESS_KEY_ID", [EnvironmentVariableTarget]::User)
[Environment]::GetEnvironmentVariable("OSS_ACCESS_KEY_SECRET", [EnvironmentVariableTarget]::User)
```

--------------------------------

### 异步 OSSClient 初始化与上传

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

展示如何配置异步客户端并执行对象上传操作。需要安装 aiohttp 依赖。

```python
import asyncio
import alibabacloud_oss_v2 as oss
import alibabacloud_oss_v2.aio as oss_aio


async def main():
    """
    Python SDK V2 异步客户端初始化配置说明：

    1. 签名版本：Python SDK V2 默认使用 V4 签名，提供更高的安全性

    2. Region配置：初始化 AsyncClient 时，必须指定阿里云 Region ID 作为请求地域标识

    3. Endpoint配置：
       - 可通过Endpoint参数自定义服务请求的访问域名
       - 当不指定 Endpoint 时，将根据 Region 自动构造公网访问域名

    4. 协议配置：
       - SDK 默认使用 HTTPS 协议构造访问域名
       - 如需使用 HTTP 协议，在指定域名时明确指定
    
    5. 异步特性：
       - 导入异步模块：import alibabacloud_oss_v2.aio as oss_aio
       - 创建异步客户端：oss_aio.AsyncClient(cfg)
       - 所有操作需要使用 await 关键字
       - 需要在 finally 块中调用 await client.close() 关闭连接
       - 需要安装 aiohttp 依赖：pip install aiohttp
    """

    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 方式一：只填写Region（推荐）
    # 必须指定Region ID，SDK会根据Region自动构造HTTPS访问域名
    cfg.region = '<region-id>'

    # # 方式二：同时填写Region和Endpoint
    # # 必须指定Region ID
    # cfg.region = '<region-id>'
    # # 填写Bucket所在地域对应的外网Endpoint
    # cfg.endpoint = '<endpoint>'

    # 使用配置好的信息创建OSS异步客户端
    client = oss_aio.AsyncClient(cfg)

    try:
        # 定义要上传的字符串内容
        text_string = "Hello, OSS!"
        data = text_string.encode('utf-8')  # 将字符串编码为UTF-8字节串

        # 执行异步上传对象的请求，指定存储空间名称、对象名称和数据内容
        # 注意：使用 await 关键字等待异步操作完成
        result = await client.put_object(
            oss.PutObjectRequest(
                bucket="Your Bucket Name",
                key="Your Object Key",
                body=data,
            )
        )

        # 输出请求的结果状态码、请求ID、ETag，用于检查请求是否成功
        print(f'status code: {result.status_code}\n'
              f'request id: {result.request_id}\n'
              f'etag: {result.etag}'
        )

    except Exception as e:
        print(f'上传失败: {e}')

    finally:
        # 关闭异步客户端连接（重要：避免资源泄漏）
        await client.close()


# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    # 使用 asyncio.run() 运行异步主函数
    asyncio.run(main())
```

--------------------------------

### CreateAccessPoint

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Creates an access point for OSS resources.

```APIDOC
## POST CreateAccessPoint

### Description
Creates a new access point.

### Method
POST

### Endpoint
CreateAccessPoint
```

--------------------------------

### 设置 STS 临时凭证环境变量

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

在不同操作系统中配置 STS 临时访问凭证的环境变量，用于后续 SDK 自动加载。

```bash
export OSS_ACCESS_KEY_ID=<STS_ACCESS_KEY_ID>
export OSS_ACCESS_KEY_SECRET=<STS_ACCESS_KEY_SECRET>
export OSS_SESSION_TOKEN=<STS_SECURITY_TOKEN>
```

```cmd
set OSS_ACCESS_KEY_ID=<STS_ACCESS_KEY_ID>
set OSS_ACCESS_KEY_SECRET=<STS_ACCESS_KEY_SECRET>
set OSS_SESSION_TOKEN=<STS_SECURITY_TOKEN>
```

--------------------------------

### 安装 RAMRoleARN 依赖

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

安装使用 RAMRoleARN 凭证所需的 alibabacloud_credentials 库。

```bash
pip install alibabacloud_credentials
```

--------------------------------

### 初始化同步 OSSClient

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

展示如何配置凭证、初始化客户端并执行简单的上传操作。

```python
import alibabacloud_oss_v2 as oss

def main():
    """
    Python SDK V2 客户端初始化配置说明：

    1. 签名版本：Python SDK V2 默认使用 V4 签名，提供更高的安全性
    2. Region配置：初始化 Client 时，必须指定阿里云 Region ID 作为请求地域标识
    3. Endpoint配置：
       - 可通过Endpoint参数自定义服务请求的访问域名
       - 当不指定 Endpoint 时，将根据 Region 自动构造公网访问域名
    4. 协议配置：
       - SDK 默认使用 HTTPS 协议构造访问域名
       - 如需使用 HTTP 协议，在指定域名时明确指定
    """

    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 方式一：只填写Region（推荐）
    # 必须指定Region ID，SDK会根据Region自动构造HTTPS访问域名
    cfg.region = '<region-id>' 

    # # 方式二：同时填写Region和Endpoint
    # # 必须指定Region ID
    # cfg.region = '<region-id>'
    # # 填写Bucket所在地域对应的外网Endpoint
    # cfg.endpoint = '<endpoint>'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 定义要上传的字符串内容
    text_string = "Hello, OSS!"
    data = text_string.encode('utf-8')  # 将字符串编码为UTF-8字节串

    # 执行上传对象的请求，指定存储空间名称、对象名称和数据内容
    result = client.put_object(oss.PutObjectRequest(
        bucket="Your Bucket Name",
        key="Your Object Key",
        body=data,
    ))

    # 输出请求的结果状态码、请求ID、ETag，用于检查请求是否成功
    print(f'status code: {result.status_code}\n'
          f'request id: {result.request_id}\n'
          f'etag: {result.etag}'
    )
```

--------------------------------

### 配置 macOS 环境变量

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

在 macOS 环境下根据 Shell 类型设置并验证 OSS 访问凭证。

```bash
echo $SHELL
```

```bash
echo "export OSS_ACCESS_KEY_ID='YOUR_ACCESS_KEY_ID'" >> ~/.zshrc
echo "export OSS_ACCESS_KEY_SECRET='YOUR_ACCESS_KEY_SECRET'" >> ~/.zshrc
```

```bash
source ~/.zshrc
```

```bash
echo $OSS_ACCESS_KEY_ID
echo $OSS_ACCESS_KEY_SECRET
```

```bash
echo "export OSS_ACCESS_KEY_ID='YOUR_ACCESS_KEY_ID'" >> ~/.bash_profile
echo "export OSS_ACCESS_KEY_SECRET='YOUR_ACCESS_KEY_SECRET'" >> ~/.bash_profile
```

```bash
source ~/.bash_profile
```

```bash
echo $OSS_ACCESS_KEY_ID
echo $OSS_ACCESS_KEY_SECRET
```

--------------------------------

### 引入 SDK 包

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

在 Python 代码中引入 SDK 模块。

```python
import alibabacloud_oss_v2 as oss
```

--------------------------------

### Implement Custom Credentials via CredentialsProviderFunc

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Uses a function wrapper to provide credentials dynamically. Replace <region-id> with your actual region.

```python
import argparse
import alibabacloud_oss_v2 as oss
import os

def main():

    def get_credentials_wrapper():
        # 返回长期凭证
        return oss.credentials.Credentials(access_key_id='access_key_id', access_key_secret='access_key_security')
        # 返回STS临时凭证
        # return oss.credentials.Credentials(access_key_id='access_key_id', access_key_secret='access_key_security', security_token='security_token')

    credentials_provider = oss.credentials.CredentialsProviderFunc(func=get_credentials_wrapper)

   # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider
    
    # 填写Bucket所在地域
    cfg.region = "<region-id>"

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用client进行后续操作...

if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### 配置 Maven 依赖

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

在 Maven 项目中引入 Hadoop OSS Connector V2 依赖。

```xml
<dependency>
    <groupId>com.aliyun</groupId>
    <artifactId>hadoop-oss</artifactId>
    <version>3.3.5-2.0.25-alpha-shade</version>
</dependency>
```

--------------------------------

### Ruby OSS Callback Server

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

Run the Ruby callback server by executing the aliyun_oss_callback_server.rb script.

```ruby
ruby aliyun_oss_callback_server.rb
```

--------------------------------

### Basic Bucket Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for fundamental bucket management like creation, deletion, and listing objects.

```APIDOC
## PutBucket

### Description
Creates a Bucket.

### Method
POST

### Endpoint
/bucketName

## DeleteBucket

### Description
Deletes a Bucket.

### Method
DELETE

### Endpoint
/bucketName

## ListObjects (GetBucket)

### Description
Lists all files (Objects) in a Bucket.

### Method
GET

### Endpoint
/bucketName

## ListObjectsV2 (GetBucketV2)

### Description
Lists all files (Objects) in a Bucket using V2.

### Method
GET

### Endpoint
/bucketName?versions

## GetBucketInfo

### Description
Retrieves information about a Bucket.

### Method
GET

### Endpoint
/bucketName

## GetBucketLocation

### Description
Retrieves the location information of a Bucket.

### Method
GET

### Endpoint
/bucketName?location

## GetBucketStat

### Description
Retrieves the storage capacity and object count of a Bucket.

### Method
GET

### Endpoint
/bucketName?stat
```

--------------------------------

### Bucket Static Website Hosting

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Endpoints for managing static website hosting configurations.

```APIDOC
## PUT /Bucket?website

### Description
设置Bucket为静态网站托管模式。

### Method
PUT

## GET /Bucket?website

### Description
查看Bucket的静态网站托管状态。

### Method
GET

## DELETE /Bucket?website

### Description
关闭Bucket的静态网站托管模式。

### Method
DELETE
```

--------------------------------

### 创建兼容 OSS 协议的外部表

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

使用 Hive DDL 创建指向 OSS 路径的外部表，支持 oss:// 和 s3a:// 协议。

```sql
CREATE EXTERNAL TABLE ods_user (
    id BIGINT,
    name STRING
)
PARTITIONED BY (dt STRING)
STORED AS PARQUET
LOCATION 'oss://my-data-lake-bucket/dw/ods/user_info/';
```

```sql
LOCATION 's3a://my-data-lake-bucket/dw/ods/user_info/';
```

--------------------------------

### InitiateMultipartUpload

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Initializes a Multipart Upload event.

```APIDOC
## POST InitiateMultipartUpload

### Description
Initializes a Multipart Upload event.

### Method
POST

### Endpoint
/ObjectName?uploads
```

--------------------------------

### 硬编码配置 STS 静态凭证

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

通过 StaticCredentialsProvider 显式设置临时访问密钥，仅建议用于测试环境。

```python
import alibabacloud_oss_v2 as oss

def main():
    # 填写获取的临时访问密钥AccessKey ID和AccessKey Secret，非阿里云账号AccessKey ID和AccessKey Secret。
    # 请注意区分STS服务获取的Access Key ID是以STS开头，如下所示。
    sts_access_key_id = 'STS.****************'
    sts_access_key_secret = 'yourAccessKeySecret'
    # 填写获取的STS安全令牌（SecurityToken）。
    sts_security_token = 'yourSecurityToken'

    # 创建静态凭证提供者，显式设置临时访问密钥AccessKey ID和AccessKey Secret，以及STS安全令牌
    credentials_provider = oss.credentials.StaticCredentialsProvider(
        access_key_id=sts_access_key_id,
        access_key_secret=sts_access_key_secret,
        security_token=sts_security_token,
    )

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作...


# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### 使用 OSS Python SDK V2 下载对象

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

演示如何配置 OSS 客户端并执行下载请求。运行前需将 <region-id> 替换为实际地域信息。

```python
import alibabacloud_oss_v2 as oss

def main():
    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 设置配置中的区域信息
    cfg.region = '<region-id>'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 执行获取对象的请求，指定存储空间名称和对象名称
    result = client.get_object(oss.GetObjectRequest(
        bucket="Your Bucket Name",  # 指定存储空间名称
        key="Your Object Key",  # 指定对象键名
    ))

    # 输出获取对象的结果信息，用于检查请求是否成功
    print(f'status code: {result.status_code},'
          f' request id: {result.request_id},'
    )

    # 使用上下文管理器确保资源释放
    with result.body as body_stream:
        data = body_stream.read()
        print(f"文件读取完成，数据长度：{len(data)} bytes")

        path = "./get-object-sample.txt"
        with open(path, 'wb') as f:
            f.write(data)
        print(f"文件下载完成，保存至路径：{path}")

# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### 构造待验证字符串

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

拼接资源路径、查询字符串和消息体以进行验证。

```python
sign_str = url_decode(path) + query_string + ‘\n’ + body
```

--------------------------------

### Bucket Basic Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for fundamental bucket management such as creation, deletion, and listing objects.

```APIDOC
## PutBucket

### Description
Creates a new Bucket.

### Method
POST

### Endpoint
/PutBucket

## DeleteBucket

### Description
Deletes a Bucket.

### Method
POST

### Endpoint
/DeleteBucket

## ListObjects (GetBucket)

### Description
Lists all objects within a Bucket.

### Method
GET

### Endpoint
/ListObjects (or /GetBucket)

## ListObjectsV2 (GetBucketV2)

### Description
Lists all objects within a Bucket using V2.

### Method
GET

### Endpoint
/ListObjectsV2 (or /GetBucketV2)

## GetBucketInfo

### Description
Retrieves information about a Bucket.

### Method
GET

### Endpoint
/GetBucketInfo

## GetBucketLocation

### Description
Retrieves the location information of a Bucket.

### Method
GET

### Endpoint
/GetBucketLocation

## GetBucketStat

### Description
Retrieves the storage capacity and object count of a Bucket.

### Method
GET

### Endpoint
/GetBucketStat
```

--------------------------------

### Pre-signed URL with Callback Parameters

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

Configure a pre-signed URL for file uploads to include callback parameters. This method is suitable for temporary access or low-sensitivity scenarios due to potential security risks from exposing callback information in the URL. Ensure the `callback` parameter is included, and `callback-var` is optional. These parameters must be part of the Canonical Query String for signature calculation.

```HTTP
PUT /your_object?OSSAccessKeyId=LTAI******************&Signature=vjby*************************************&Expires=1682484377&callback-var=eyJ4OnVpZCI6ICIxMjM0NSIsICJ4Om9yZGVyX2lkIjogIjY3ODkwIn0=&callback= HTTP/1.1
Host: callback-test.oss-cn-hangzhou.aliyuncs.com
Date: Wed, 26 Apr 2023 03:46:17 GMT
Content-Length: 5
Content-Type: text/plain
```

--------------------------------

### Configure Financial Cloud Domain

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Set region and endpoint specifically for Financial Cloud environments.

```python
import alibabacloud_oss_v2 as oss

def main():
    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 填写Region和Endpoint
    # 填写Bucket所在地域。以华东1 金融云为例，Region填写为cn-hangzhou-finance
    cfg.region = 'cn-hangzhou-finance'
    # 填写Bucket所在地域对应的内网Endpoint。以华东1 金融云为例，Endpoint填写为'https://oss-cn-hzjbp-a-internal.aliyuncs.com',
    # 如需指定为http协议，请在指定域名时填写为'http://oss-cn-hzjbp-a-internal.aliyuncs.com'
    cfg.endpoint = 'https://oss-cn-hzjbp-a-internal.aliyuncs.com'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作...

# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### Redundancy Transition API

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

API for creating data redundancy transition configurations for OSS buckets.

```APIDOC
## CreateBucketDataRedundancyTransition

### Description
Creates a data redundancy transition configuration for an OSS bucket.

### Method
POST

### Endpoint
/bucketName?redundancyTransition&create

### Request Body
- **redundancyTransition** (object) - Required - Redundancy transition configuration.
  - **sourceStorageClass** (string) - Required - The source storage class.
  - **targetStorageClass** (string) - Required - The target storage class.
  - **transitionTime** (string) - Required - The time when the transition should occur (e.g., `2023-12-31T23:59:59Z`).

### Response
#### Success Response (200)
- **No content**
```

--------------------------------

### Configure Hadoop core-site.xml for S3A Compatibility

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

Add these properties to core-site.xml to enable S3A to OSS mapping. Ensure the endpoint is set to an internal address for better performance.

```xml
<configuration>
    <!-- 启用 S3A 兼容模式下的 OSS 实现-->
    <property>
        <name>fs.AbstractFileSystem.s3a.impl</name>
        <value>org.apache.hadoop.fs.aliyun.oss.v2.OSSWithS3A</value>
    </property>

    <!-- 指定 s3a 文件系统实现类 -->
    <property>
        <name>fs.s3a.impl</name>
        <value>org.apache.hadoop.fs.aliyun.oss.v2.AliyunOSSPerformanceFileSystem</value>
    </property>

    <!-- S3A 访问密钥（使用阿里云 AccessKey） -->
    <property>
        <name>fs.s3a.access.key</name>
        <value>YourAccessKeyId</value>
    </property>
    <property>
        <name>fs.s3a.secret.key</name>
        <value>YourAccessKeySecret</value>
    </property>

    <!-- OSS Endpoint（建议使用内网地址以提升性能） -->
    <property>
        <name>fs.s3a.endpoint</name>
        <value>oss-cn-hangzhou-internal.aliyuncs.com</value>
    </property>

    <!-- 可选：关闭 SSL 以避免证书问题 -->
    <property>
        <name>fs.s3a.connection.ssl.enabled</name>
        <value>false</value>
    </property>
</configuration>
```

--------------------------------

### PutBucketDataAccelerator

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Creates or updates the configuration of an OSS Accelerator for a bucket.

```APIDOC
## PUT /?dataAccelerator

### Description
Create an OSS Accelerator or modify its configuration.

### Method
PUT

### Endpoint
/?dataAccelerator
```

--------------------------------

### Perform Large File I/O Performance Test

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

Generate a 20GB file and measure upload/download performance using specific buffer sizes.

```bash
# 生成测试文件
dd if=/dev/zero of=/tmp/large-file-20G bs=1M count=20480

# 上传至 OSS（支持 oss:// 或 s3a://）
hadoop fs -D io.file.buffer.size=4194304 -copyFromLocal -f /tmp/large-file-20G oss://your-bucket/path/20GB-file.img

# 下载测试
hadoop fs -D io.file.buffer.size=4194304 -copyToLocal -f oss://your-bucket/path/20GB-file.img /dev/null
```

--------------------------------

### 配置政务云域名

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

通过设置Region和Endpoint来连接政务云环境的OSS服务。

```python
import alibabacloud_oss_v2 as oss

def main():
    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 填写Region和Endpoint
    # 填写Bucket所在地域。以华北2 阿里政务云1为例，Region填写为cn-north-2-gov-1
    cfg.region = 'cn-north-2-gov-1'
    # 填写Bucket所在地域对应的内网Endpoint。以华北2 阿里政务云1为例，Endpoint填写为'https://oss-cn-north-2-gov-1-internal.aliyuncs.com',
    # 如需指定为http协议，请在指定域名时填写为'http://oss-cn-north-2-gov-1-internal.aliyuncs.com'
    cfg.endpoint = 'https://oss-cn-north-2-gov-1-internal.aliyuncs.com'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作...

# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### Configure Custom Domain for OSS

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Use a custom domain to access OSS objects, enabling direct file previews and CDN acceleration. Ensure `use_cname` is set to `True`.

```python
import alibabacloud_oss_v2 as oss

def main():
    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    # 请填写您的自定义域名。例如www.example-***.com
    cfg.endpoint = 'https://www.example-***.com'

    # 请注意，设置true开启CNAME选项，否则无法使用自定义域名
    cfg.use_cname = True

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作... 

# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数

```

--------------------------------

### Configure Transfer Acceleration

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Enable transfer acceleration to optimize data transmission speeds.

```python
import alibabacloud_oss_v2 as oss

def main():
    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 方式一： 填写Region并设置use_accelerate_endpoint为true
    # 填写Bucket所在地域
    cfg.region = '<region-id>'
    cfg.use_accelerate_endpoint = True

    # # 方式二： 直接填写Region和传输加速Endpoint
    # # 填写Bucket所在地域
    # cfg.region = '<region-id>'
    # # 填写Bucket所在地域对应的传输加速Endpoint
    # cfg.endpoint = 'https://oss-accelerate.aliyuncs.com'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作...

# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### Configure Internal Endpoint

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Access OSS resources within the same region using internal endpoints to reduce costs and improve speed.

```python
import alibabacloud_oss_v2 as oss

def main():
    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 方式一： 填写Region并设置use_internal_endpoint为true
    # 填写Bucket所在地域
    cfg.region = '<region-id>'
    cfg.use_internal_endpoint = True

    # # 方式二： 直接填写Region和Endpoint
    # # 填写Bucket所在地域
    # cfg.region = '<region-id>'
    # # 填写Bucket所在地域对应的内网Endpoint
    # cfg.endpoint = '<endpoint>'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作...

# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### 安装 OSS Python SDK V2

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

使用 pip 安装 SDK 代码包。

```bash
pip install alibabacloud-oss-v2
```

--------------------------------

### PUT / (CreateBucket)

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

Creates a new storage bucket in the specified region.

```APIDOC
## PUT /

### Description
Creates a new storage bucket (Bucket) in the specified region.

### Method
PUT

### Endpoint
/

### Request Body
- **StorageClass** (string) - Required - The storage class of the bucket (e.g., Standard).
- **DataRedundancyType** (string) - Required - The data redundancy type (e.g., LRS).

### Request Example
PUT / HTTP/1.1
Host: oss-example.oss-cn-hangzhou.aliyuncs.com
Date: Thu, 17 Apr 2025 03:15:40 GMT
x-oss-acl: private
Authorization: OSS4-HMAC-SHA256 Credential=LTAI.../20250417/cn-hangzhou/oss/aliyun_v4_request,Signature=...

<?xml version="1.0" encoding="UTF-8"?>
<CreateBucketConfiguration>
    <StorageClass>Standard</StorageClass>
    <DataRedundancyType>LRS</DataRedundancyType>    
</CreateBucketConfiguration>

### Response
#### Success Response (200)
- **x-oss-request-id** (string) - The unique ID for the request.
- **Location** (string) - The path of the created bucket.

#### Response Example
HTTP/1.1 200 OK
x-oss-request-id: 534B371674E88A4D8906****
Date: Fri, 24 Feb 2017 03:15:40 GMT
Content-Length: 0
Connection: keep-alive
Server: AliyunOSS
Location: /oss-example
```

--------------------------------

### Configure HTTP Protocol

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Disable SSL to use HTTP instead of HTTPS by setting cfg.disable_ssl to True.

```python
import alibabacloud_oss_v2 as oss

def main():
    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    # 设置不使用https请求
    cfg.disable_ssl = True

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作...

# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### Configure Prefix Matching Acceleration

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

Enable caching for files under a specified path prefix. Useful for caching intermediate results of hot queries.

```xml
<keyPrefixes>
    <keyPrefix>tpcds/q9/</keyPrefix>
    <keyPrefix>tmp/report_</keyPrefix>
</keyPrefixes>
```

--------------------------------

### OSS 客户端配置参数示例

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

列举了 OSS 客户端支持的各项配置参数及其对应的代码示例。

```python
oss.config.Config(region="<region-id>")
oss.config.Config(credentials_provider=provider)
oss.config.Config(endpoint="oss-<region-id>.aliyuncs.com")
oss.config.Config(http_client=customClient)
oss.config.Config(retry_max_attempts=5)
oss.config.Config(retryer=customRetryer)
oss.config.Config(connect_timeout=20)
oss.config.Config(readwrite_timeout=30)
oss.config.Config(insecure_skip_verify=true)
oss.config.Config(enabled_redirect=true)
oss.config.Config(proxy_host="_http://user:passswd@proxy.example-***.com_")
oss.config.Config(signature_version="v1")
oss.config.Config(disable_ssl=true)
oss.config.Config(use_path_style=true)
oss.config.Config(use_cname=true)
oss.config.Config(use_dualstack_endpoint=true)
oss.config.Config(use_accelerate_endpoint=true)
oss.config.Config(use_internal_endpoint=true)
oss.config.Config(disable_upload_crc64_check=true)
oss.config.Config(disable_download_crc64_check=true)
oss.config.Config(additional_headers=["content-length"])
oss.config.Config(user_agent="user identifier")
```

--------------------------------

### Bucket Static Website Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for configuring and managing static website hosting for Buckets.

```APIDOC
## PutBucketWebsite

### Description
Configures a Bucket for static website hosting.

### Method
POST

### Endpoint
/PutBucketWebsite

## GetBucketWebsite

### Description
Retrieves the static website hosting status of a Bucket.

### Method
GET

### Endpoint
/GetBucketWebsite

## DeleteBucketWebsite

### Description
Disables static website hosting for a Bucket.

### Method
POST

### Endpoint
/DeleteBucketWebsite
```

--------------------------------

### 公共请求头 (Common Request Headers)

Source: https://help.aliyun.com/zh/oss/developer-reference/common-http-headers

OSS RESTful 接口中可被所有请求使用的公共请求头定义。

```APIDOC
## 公共请求头

| 名称 | 类型 | 描述 |
| :--- | :--- | :--- |
| Authorization | 字符串 | 在非匿名请求中，用于验证请求合法性的认证信息。 |
| Content-Length | 字符串 | HTTP 请求内容长度。 |
| Content-Type | 字符串 | HTTP 请求内容类型。 |
| Date | 字符串 | HTTP 1.1 协议中规定的 GMT 时间。 |
| Host | 字符串 | 访问域名，支持 Bucket 域名、接入点别名域名及云盒域名。 |
| x-oss-security-token | 字符串 | 仅在使用 STS 临时授权账号访问 OSS 时需要配置。 |
```

--------------------------------

### 构造 Callback 参数

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

定义应用服务器地址及回调内容格式。最简配置仅需 URL 和 Body，进阶配置支持 Host、BodyType 和 SNI 设置。

```json
{
"callbackUrl":"http://oss-demo.aliyuncs.com:23450",
"callbackBody":"bucket=${bucket}&object=${object}&my_var=${x:my_var}"
}
```

```json
{
"callbackUrl":"http://oss-demo.aliyuncs.com:23450",
"callbackHost":"oss-cn-hangzhou.aliyuncs.com",
"callbackBody":"bucket=${bucket}&object=${object}&my_var=${x:my_var}",
"callbackBodyType":"application/x-www-form-urlencoded",
"callbackSNI":false
}
```

--------------------------------

### 脚本入口定义

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

定义脚本直接运行时的入口点。

```python
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### Configure Proxy Server

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Route requests through a proxy server by setting the proxy_host configuration.

```python
import alibabacloud_oss_v2 as oss

def main():
    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    # 设置user_agent
    cfg.user_agent = 'aliyun-sdk-python'

    # 设置代理服务器
    cfg.proxy_host = 'http://user:passswd@proxy.example-***.com'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作...

# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### Live Channel Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

A collection of API operations for managing Live Channels in OSS.

```APIDOC
## Live Channel API Operations

### Description
This section lists the available API operations for managing Live Channels, including creating, listing, updating status, and retrieving information or history.

### Operations
- **PutLiveChannel**: Create a LiveChannel.
- **PutLiveChannelStatus**: Switch the status of a LiveChannel.
- **GetLiveChannelStat**: Retrieve push stream status information for a LiveChannel.
- **GetLiveChannelInfo**: Retrieve configuration information for a LiveChannel.
- **GetLiveChannelHistory**: Retrieve push stream history for a LiveChannel.
- **ListLiveChannel**: List all LiveChannels.
- **DeleteLiveChannel**: Delete a LiveChannel.
- **PostVodPlaylist**: Generate a VOD playlist.
- **GetVodPlaylist**: Retrieve a VOD playlist.
```

--------------------------------

### Configure File Size Range Acceleration

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

Accelerate small files within a specified size range (0-50MB). This covers common scanning operations and is useful for metadata and small partition files.

```xml
<IOSizeRanges>
    <ioSizeRange>
        <ioType>SIZE</ioType>
        <minIOSize>0</minIOSize>
        <maxIOSize>52428800</maxIOSize>
    </ioSizeRange>
</IOSizeRanges>
```

--------------------------------

### Python 3 签名验证完整示例

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

使用M2Crypto库在应用服务器中验证OSS回调签名的完整实现。

```python
import http.client
import base64
import hashlib
import urllib.request
import urllib.parse
import socket
from http.server import BaseHTTPRequestHandler, HTTPServer
from M2Crypto import RSA
from M2Crypto import BIO

def get_local_ip():
    try:
        csock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
        csock.connect(('8.8.8.8', 80))
        (addr, port) = csock.getsockname()
        csock.close()
        return addr
    except socket.error:
        return ""

class MyHTTPRequestHandler(BaseHTTPRequestHandler):
    '''
    def log_message(self, format, *args):
        return
    '''
    def do_POST(self):
        #get public key
        pub_key_url = ''
        try:
            pub_key_url_base64 = self.headers['x-oss-pub-key-url']
            pub_key_url = base64.b64decode(pub_key_url_base64).decode()
            if not pub_key_url.startswith("http://gosspublic.alicdn.com/") and not pub_key_url.startswith("https://gosspublic.alicdn.com/"):
                self.send_response(400)
                self.end_headers()
                return
            url_reader = urllib.request.urlopen(pub_key_url)
            #you can cache it,recommend caching public key content based on the public key address
            pub_key = url_reader.read() 
        except Exception as e:
            print('pub_key_url : ' + pub_key_url)
            print('Get pub key failed! Error:', str(e))
            self.send_response(400)
            self.end_headers()
            return
        
        #get authorization
        authorization_base64 = self.headers['authorization']
        authorization = base64.b64decode(authorization_base64)
        #get callback body
        content_length = self.headers['content-length']
        callback_body = self.rfile.read(int(content_length))
        #compose authorization string
        auth_str = ''
        pos = self.path.find('?')
        if -1 == pos:
            auth_str = urllib.parse.unquote(self.path) + '\n' + callback_body.decode()
        else:
            auth_str = urllib.parse.unquote(self.path[0:pos]) + self.path[pos:] + '\n' + callback_body.decode()
        print(auth_str)
        #verify authorization
        auth_md5 = hashlib.md5(auth_str.encode()).digest()
        bio = BIO.MemoryBuffer(pub_key)
        rsa_pub = RSA.load_pub_key_bio(bio)
        try:
            result = rsa_pub.verify(auth_md5, authorization, 'md5')
        except:
            result = False
        if not result:
            print('Authorization verify failed!')
            print('Public key : %s' % (pub_key))
            print('Auth string : %s' % (auth_str))
            self.send_response(400)
            self.end_headers()
            return
        
        #do something according to callback_body
        #response to OSS
        resp_body = '{"Status":"OK"}'
        self.send_response(200)
        self.send_header('Content-Type', 'application/json')
```

--------------------------------

### Configure Custom Domain

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Set a specific endpoint URL for custom domain access.

```python
import alibabacloud_oss_v2 as oss

def main():
    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载SDK的默认配置，并设置凭证提供者
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    # 请填写您的专有域。例如：https://service.corp.example.com
    cfg.endpoint = 'https://service.corp.example.com'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作...

# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### Configure Accelerator Endpoint

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

Set the accelerator endpoint in core-site.xml for private network access. This is a required property for enabling acceleration.

```xml
<property>
    <name>fs.oss.acc.endpoint</name>
    <value>https://cn-hangzhou-j-internal.oss-data-acc.aliyuncs.com</value>
</property>
```

--------------------------------

### 公共响应头 (Common Response Headers)

Source: https://help.aliyun.com/zh/oss/developer-reference/common-http-headers

OSS RESTful 接口中所有响应均包含的公共响应头定义。

```APIDOC
## 公共响应头

| 名称 | 类型 | 描述 |
| :--- | :--- | :--- |
| Content-Length | 字符串 | HTTP 响应内容长度。 |
| Connection | 枚举 | 标明客户端和 OSS 服务器之间的链接状态（keep-alive 或 close）。 |
| Date | 字符串 | HTTP 1.1 协议中规定的 GMT 时间。 |
| ETag | 字符串 | 用于标识 Object 内容的唯一标识符。 |
| Server | 字符串 | 生成 Response 的服务器，默认为 AliyunOSS。 |
| x-oss-request-id | 字符串 | 用于唯一标识该次请求的 UUID，用于排查问题。 |
```

--------------------------------

### Bucket Logging Management

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Endpoints for managing access logging configurations.

```APIDOC
## PUT /Bucket?logging

### Description
开启Bucket访问日志记录功能。

### Method
PUT

## GET /Bucket?logging

### Description
查看Bucket的访问日志配置情况。

### Method
GET

## DELETE /Bucket?logging

### Description
关闭Bucket访问日志记录功能。

### Method
DELETE
```

--------------------------------

### Configure Operation Type Acceleration

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

Enable acceleration for specific operations, currently only 'getObject' is supported. This is suitable for read-intensive tasks.

```xml
<operations>
    <operation>getObject</operation>
</operations>
```

--------------------------------

### PostObject 接口回调参数传递

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

PostObject 接口需将 callback 和自定义参数作为独立的表单项传入。

```text
--9431149156168
Content-Disposition: form-data; name="callback"

```

```json
{
  "x:uid": "12345",
  "x:order_id": "67890"
}
```

```text
--9431149156168
Content-Disposition: form-data; name="x:uid"
12345
--9431149156168
Content-Disposition: form-data; name="x:order_id"
67890
```

--------------------------------

### 执行签名验证

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

使用MD5哈希和RSA公钥验证签名。

```python
result = rsa_verify(public_key, md5(sign_str), signature)
```

--------------------------------

### OSS Accelerator Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing OSS Accelerator configurations.

```APIDOC
## PUT /<bucket_name>/data-accelerator

### Description
Creates or modifies the OSS Accelerator configuration for a bucket.

### Method
PUT

### Endpoint
`/<bucket_name>/data-accelerator`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Request Body
- **DataAcceleratorConfiguration** (json) - Required - The data accelerator configuration.
  - **Enabled** (boolean) - Required - Whether to enable data accelerator.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /<bucket_name>/data-accelerator

### Description
Retrieves the OSS Accelerator information for a bucket.

### Method
GET

### Endpoint
`/<bucket_name>/data-accelerator`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Response
#### Success Response (200)
- **DataAcceleratorConfiguration** (json) - The data accelerator configuration.

### Response Example
```json
{
  "DataAcceleratorConfiguration": {
    "Enabled": true,
    "Endpoint": "accelerator.oss.aliyuncs.com"
  }
}
```
```

```APIDOC
## DELETE /<bucket_name>/data-accelerator

### Description
Deletes the OSS Accelerator configuration for a bucket.

### Method
DELETE

### Endpoint
`/<bucket_name>/data-accelerator`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Response
#### Success Response (200)
(No content)
```

--------------------------------

### 计算签名公式

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

OSS生成签名时使用的基础公式。

```text
authorization = base64_encode(rsa_sign(private_key, url_decode(path) + query_string + '\n' + body, md5))
```

--------------------------------

### GetObject

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Retrieves an Object from the OSS bucket.

```APIDOC
## GET GetObject

### Description
Retrieves an Object from the OSS bucket.

### Method
GET

### Endpoint
/ObjectName
```

--------------------------------

### Execute Query on S3A-backed Table

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

Run standard SQL queries against the table. The connector automatically routes the request to the underlying OSS storage.

```sql
SELECT count(*) FROM ods_user WHERE dt = '20250405';
```

--------------------------------

### Configure Tail/Head Access Optimization

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

Cache file headers (metadata) and tails (log appends) for structured file parsing. Caches the first 2MB for headers and the last 2MB for tails.

```xml
<IOSizeRanges>
    <ioSizeRange>
        <ioType>HEAD</ioType>
        <ioSize>2097152</ioSize>
    </ioSizeRange>

    <ioSizeRange>
        <ioType>TAIL</ioType>
        <ioSize>2097152</ioSize>
    </ioSizeRange>
</IOSizeRanges>
```

--------------------------------

### Bucket Transfer Acceleration

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for configuring and retrieving transfer acceleration settings for a bucket.

```APIDOC
## PutBucketTransferAcceleration

### Description
Configures transfer acceleration for a Bucket.

### Method
PUT

### Endpoint
/bucketName?transferAcceleration

## GetBucketTransferAcceleration

### Description
Retrieves the transfer acceleration configuration for a Bucket.

### Method
GET

### Endpoint
/bucketName?transferAcceleration
```

--------------------------------

### Bucket Redundancy Transition Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing storage redundancy transition tasks for Buckets.

```APIDOC
## CreateBucketDataRedundancyTransition

### Description
Creates a storage redundancy transition task.

### Method
POST

### Endpoint
/CreateBucketDataRedundancyTransition

## GetBucketDataRedundancyTransition

### Description
Retrieves a storage redundancy transition task.

### Method
GET

### Endpoint
/GetBucketDataRedundancyTransition

## DeleteBucketDataRedundancyTransition

### Description
Deletes a storage redundancy transition task.

### Method
POST

### Endpoint
/DeleteBucketDataRedundancyTransition

## ListUserDataRedundancyTransition

### Description
Lists all storage redundancy transition tasks for the current user.

### Method
GET

### Endpoint
/ListUserDataRedundancyTransition

## ListBucketDataRedundancyTransition

### Description
Lists all storage redundancy transition tasks for a specific Bucket.

### Method
GET

### Endpoint
/ListBucketDataRedundancyTransition
```

--------------------------------

### Implement Custom Credentials via CredentialsProvider Interface

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Extends the CredentialsProvider class to define custom credential retrieval logic. Replace <region-id> and placeholders with actual values.

```python
# -*- coding: utf-8 -*-
import alibabacloud_oss_v2 as oss

class CredentialProviderWrapper(oss.credentials.CredentialsProvider):
    def get_credentials(self):
        # TODO
        # 自定义访问凭证的获取方法

        # 返回长期凭证access_key_id, access_key_secrect
        return oss.credentials.Credentials('<access_key_id>', '<access_key_secrect>')

        # 返回 STS临时凭证access_key_id, access_key_secrect, token
        # 对于临时凭证，需要根据过期时间，刷新凭证。
        # return oss.credentials.Credentials('<access_key_id>', '<access_key_secrect>', '<token>');


def main():
    # 创建凭证提供者，用于动态加载凭证
    credentials_provider = CredentialProviderWrapper()

    # 加载OSS SDK的默认配置
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    client = oss.Client(cfg)

    # 使用client进行后续操作...


if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### Configure Suffix Matching Acceleration

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

Enable caching for files with specific extensions. Suitable for caching files of a uniform format.

```xml
<keySuffixes>
    <keySuffix>.parquet</keySuffix>
    <keySuffix>.orc</keySuffix>
    <keySuffix>.csv</keySuffix>
</keySuffixes>
```

--------------------------------

### 自定义 HTTPClient 配置

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

通过替换默认的HTTP客户端并调整连接超时、读写超时及最大连接数等参数，以满足特定网络环境需求。

```python
import alibabacloud_oss_v2 as oss
from typing import Dict, Any

def main():
    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载默认的配置信息
    cfg = oss.config.load_default()

    # 设置HTTP客户端的参数
    kwargs: Dict[str, Any] = {}

    # 设置session
    # kwargs["session"] = requests.Session()

    # 设置adapter
    # kwargs["adapter"] = HTTPAdapter()

    # 是否跳过证书检查，默认不跳过
    # kwargs["insecure_skip_verify"] = False

    # 是否打开启HTTP重定向，默认不启用
    # kwargs["enabled_redirect"] = False

    # 设置代理服务器
    # kwargs["proxy_host"] = config.proxy_host

    # 设置块大小
    # kwargs["block_size"] = 16 * 1024

    # 连接超时, 默认值 10秒
    kwargs["connect_timeout"] = 30

    # 应用读写数据的超时时间, 默认值 20秒
    kwargs["readwrite_timeout"] = 30

    # 最大连接数，默认值 20
    kwargs["max_connections"] = 1024

    # 创建HTTP客户端，并传入HTTP客户端参数
    cfg.http_client = oss.transport.RequestsHttpClient(**kwargs)

    # 设置凭证提供者
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作...


# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### PutObject

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Uploads an Object to the OSS bucket.

```APIDOC
## PUT PutObject

### Description
Uploads an Object to the OSS bucket.

### Method
PUT

### Endpoint
/ObjectName
```

--------------------------------

### Bucket Image Style Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for creating, retrieving, listing, and deleting image styles for Buckets.

```APIDOC
## PutStyle

### Description
Adds a new image style for a Bucket.

### Method
POST

### Endpoint
/PutStyle

## GetStyle

### Description
Retrieves information about a specific image style within a Bucket.

### Method
GET

### Endpoint
/GetStyle

## ListStyle

### Description
Lists all image styles created for a Bucket.

### Method
GET

### Endpoint
/ListStyle

## DeleteStyle

### Description
Deletes a specific image style from a Bucket.

### Method
POST

### Endpoint
/DeleteStyle
```

--------------------------------

### TLS Configuration API

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for managing TLS version settings for OSS buckets.

```APIDOC
## PutBucketHttpsConfig

### Description
Enables or disables TLS version settings for an OSS bucket.

### Method
PUT

### Endpoint
/bucketName?httpsConfig

### Request Body
- **httpsConfig** (object) - Required - HTTPS configuration.
  - **tlsVersion** (string) - Required - The minimum TLS version. Possible values: `TLSv1.0`, `TLSv1.1`, `TLSv1.2`.

### Response
#### Success Response (200)
- **No content**

## GetBucketHttpsConfig

### Description
Retrieves the TLS version settings for an OSS bucket.

### Method
GET

### Endpoint
/bucketName?httpsConfig

### Response
#### Success Response (200)
- **httpsConfig** (object)
  - **tlsVersion** (string) - The minimum TLS version. Possible values: `TLSv1.0`, `TLSv1.1`, `TLSv1.2`.
```

--------------------------------

### Bucket Lifecycle Management

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for configuring and managing lifecycle rules for objects within a bucket.

```APIDOC
## PutBucketLifecycle

### Description
Sets lifecycle rules for Objects in a Bucket.

### Method
PUT

### Endpoint
/bucketName?lifecycle

## GetBucketLifecycle

### Description
Retrieves the lifecycle rules for Objects in a Bucket.

### Method
GET

### Endpoint
/bucketName?lifecycle

## DeleteBucketLifecycle

### Description
Deletes the lifecycle rules for Objects in a Bucket.

### Method
DELETE

### Endpoint
/bucketName?lifecycle
```

--------------------------------

### Image Style API

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for managing image processing styles for OSS buckets.

```APIDOC
## PutStyle

### Description
Adds a new image processing style for an OSS bucket.

### Method
PUT

### Endpoint
/bucketName/styleName

### Request Body
- **style** (object) - Required - Image style configuration.
  - **content** (string) - Required - The style definition (e.g., `image/resize,w_100/format,jpg`).

### Response
#### Success Response (200)
- **No content**

## GetStyle

### Description
Retrieves the specified image processing style information for an OSS bucket.

### Method
GET

### Endpoint
/bucketName/styleName

### Response
#### Success Response (200)
- **style** (object)
  - **content** (string) - The style definition.

## ListStyle

### Description
Retrieves all created image processing styles for an OSS bucket.

### Method
GET

### Endpoint
/bucketName?style

### Response
#### Success Response (200)
- **styles** (array)
  - **style** (object)
    - **name** (string) - The name of the style.
    - **content** (string) - The style definition.

## DeleteStyle

### Description
Deletes a specified image processing style from an OSS bucket.

### Method
DELETE

### Endpoint
/bucketName/styleName

### Response
#### Success Response (200)
- **No content**
```

--------------------------------

### Configure Anonymous Access

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Configures the client for anonymous access to public OSS resources. Replace <region-id> with your actual region.

```python
import alibabacloud_oss_v2 as oss

# 创建匿名凭证提供者
credentials_provider = oss.credentials.AnonymousCredentialsProvider()

# 配置客户端
cfg = oss.config.load_default()
cfg.credentials_provider = credentials_provider
# 填写Bucket所在地域
cfg.region = '<region-id>'

# 创建OSS客户端
client = oss.Client(cfg)
```

--------------------------------

### Execute TPC-DS Benchmark Query

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

Run a standard TPC-DS query using Spark Beeline to validate end-to-end performance.

```bash
$SPARK_HOME/bin/beeline -u jdbc:hive2://localhost:10001/tpcds_bin_partitioned_orc_5000 -f q9.sql
```

--------------------------------

### Vector Bucket Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for creating, retrieving, listing, and deleting vector buckets.

```APIDOC
## POST /PutVectorBucket

### Description
Creates a vector bucket.

### Method
POST

### Endpoint
/PutVectorBucket

## GET /GetVectorBucket

### Description
Retrieves detailed information about a vector bucket.

### Method
GET

### Endpoint
/GetVectorBucket

## GET /ListVectorBuckets

### Description
Lists all vector buckets under the current account.

### Method
GET

### Endpoint
/ListVectorBuckets

## DELETE /DeleteVectorBucket

### Description
Deletes a vector bucket.

### Method
DELETE

### Endpoint
/DeleteVectorBucket
```

--------------------------------

### Configure Timeout Settings for OSS Client

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Adjust connection and read/write timeouts for the OSS client to manage request duration. Default connection timeout is 10 seconds, and read/write timeout is 20 seconds.

```python
import alibabacloud_oss_v2 as oss
from typing import Dict, Any

def main():
    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载默认的配置信息
    cfg = oss.config.load_default()

    # 设置建立连接的超时时间, 默认值 10秒
    cfg.connect_timeout = 30

    # 设置应用读写数据的超时时间, 默认值 20秒
    cfg.readwrite_timeout = 30

    # 设置凭证提供者
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作... 

# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数

```

--------------------------------

### Bucket Operations API

Source: https://help.aliyun.com/zh/oss/developer-reference/bucket-operations

This section outlines the various API categories and their corresponding operations for managing Alibaba Cloud Object Storage Service (OSS) Buckets.

```APIDOC
## Bucket Operations API Reference

This document provides an overview of the APIs available for managing Buckets in Alibaba Cloud Object Storage Service (OSS).

### API Categories and Descriptions

- **Basic Operations**: Covers fundamental Bucket operations such as creating, deleting, and listing Buckets and files.
- **Permissions (ACL)**: APIs for managing Bucket Access Control Lists (ACLs), including setting and retrieving ACLs.
- **Block Public Access**: APIs to control and prevent public access to data, applicable globally or at the Bucket level.
- **Policy**: APIs for managing Bucket Policies, including setting and deleting policies.
- **Versioning**: APIs related to version control for objects within a Bucket, such as setting versioning and listing object versions.
- **Referer**: APIs for configuring and managing Referer settings to prevent hotlinking.
- **TLS**: APIs for configuring and managing Transport Layer Security (TLS) settings for secure data transmission.
- **CORS**: APIs for configuring Cross-Origin Resource Sharing (CORS) rules for Buckets.
- **WORM (Compliance)**: APIs for managing Worm (Write Once, Read Many) compliance retention policies.
- **Encryption**: APIs for configuring and managing encryption rules for data stored in Buckets.
- **DDoS Protection**: APIs for managing DDoS protection instances and their states.
- **Replication**: APIs for configuring data replication rules between Buckets.
- **Lifecycle**: APIs for managing object lifecycle rules, such as transitioning or deleting objects based on age or other criteria.
- **Inventory**: APIs for configuring and viewing inventory reports for Buckets.
- **Transfer Acceleration**: APIs for configuring and managing transfer acceleration for faster data uploads and downloads.
- **Website**: APIs for configuring and managing static website hosting for Buckets.
- **Access Monitor**: APIs for enabling and configuring access monitoring for Buckets.
- **Redundancy Transition**: APIs for managing storage redundancy transition tasks.
- **Archive Direct Read**: APIs for enabling and configuring Archive Direct Read functionality.
- **Data Indexing**: APIs for enabling and managing data indexing and retrieval.
- **Custom Domain (CNAME)**: APIs for configuring and managing custom domain names for Buckets.
- **Tags**: APIs for managing tags associated with Buckets.
- **Request Payment**: APIs for configuring and managing requester pays settings for Buckets.
- **Resource Group**: APIs for configuring and querying resource group information for Buckets.
- **Logging**: APIs for enabling and configuring access logging for Buckets.
- **Style**: APIs for managing image styles for on-the-fly image processing.
- **Access Point**: APIs for creating and managing access points for Buckets.
- **Object FC Access Point**: APIs for creating and managing Object FC access points.

For specific API details, please refer to the relevant sections within the Alibaba Cloud OSS documentation.
```

--------------------------------

### OSS Callback Usage Limitations

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

Understand the restrictions and requirements for using the OSS Callback feature.

```APIDOC
## Usage Limitations

### Region Support
Callback settings are supported in the following regions: East China 1 (Hangzhou), East China 2 (Shanghai), North China 1 (Qingdao), North China 2 (Beijing), North China 3 (Zhangjiakou), North China 5 (Hohhot), North China 6 (Wulanchabu), South China 1 (Shenzhen), South China 2 (Heyuan), South China 3 (Guangzhou), Southwest 1 (Chengdu), China (Hong Kong), US (Silicon Valley), US (Virginia), Japan (Tokyo), Singapore, Malaysia (Kuala Lumpur), Indonesia (Jakarta), Philippines (Manila), Germany (Frankfurt), UK (London), UAE (Dubai).

### Callback Behavior
*   Callback requests must be responded to within 5 seconds; timeouts are considered failures.
*   Callback failures do not impact the success of file uploads.
*   Callback failures are not automatically retried.

### Interface Support
The `PutObject`, `PostObject`, and `CompleteMultipartUpload` interfaces support setting Callbacks. The V2 SDK's file upload manager and pre-signed URLs, which are built upon these base interfaces, also support setting Callbacks.
```

--------------------------------

### GetBucketDataAccelerator

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Queries the information of an OSS Accelerator associated with a bucket.

```APIDOC
## GET /?dataAccelerator

### Description
Query the information of the OSS Accelerator.

### Method
GET

### Endpoint
/?dataAccelerator
```

--------------------------------

### Initialize OSS Client with ECS RAM Role

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Recommended for applications running on ECS, ECI, or Kubernetes worker nodes. This method automatically refreshes STS tokens without manual AK/STS management. Ensure the ECS instance or pod has an associated RAM role.

```python
from alibabacloud_credentials.client import Client
from alibabacloud_credentials.models import Config
import alibabacloud_oss_v2 as oss

def main():
    config = Config(
        type='ecs_ram_role',      # 访问凭证类型。固定为ecs_ram_role。
        role_name='EcsRoleExample'    # 为ECS授予的RAM角色的名称。可选参数。如果不设置，将自动检索。强烈建议设置，以减少请求。
    )

    cred_client = Client(config)

    def get_credentials_wrapper():
        cred = cred_client.get_credential()
        return oss.credentials.Credentials(access_key_id=cred.access_key_id, access_key_secret=cred.access_key_secret, security_token=cred.security_token)

    # 创建凭证提供者，用于动态加载凭证
    credentials_provider = oss.credentials.CredentialsProviderFunc(func=get_credentials_wrapper)

    # 加载OSS SDK的默认配置
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    # 创建OSS客户端实例
    client = oss.Client(cfg)

    # 使用client进行后续操作... 


# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数

```

--------------------------------

### 解码签名

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

从请求头中获取并解码签名。

```python
signature = base64_decode(authorization头的值)
```

--------------------------------

### GetBucketDataRedundancyTransition

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Retrieves the details of a storage redundancy transition task.

```APIDOC
## GET GetBucketDataRedundancyTransition

### Description
Retrieves the storage redundancy transition task information.

### Method
GET

### Endpoint
GetBucketDataRedundancyTransition
```

--------------------------------

### Configure Retry Strategies for OSS Client

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Customize retry attempts, backoff delays, and retry algorithms for handling transient errors. The default retryer uses `StandardRetryer` with `max_attempts=3`, `max_backoff=20`, `base_delay=0.2`, and `FullJitter`.

```python
import alibabacloud_oss_v2 as oss

def main():
    """
    SDK 重试策略配置说明：

    默认重试策略：
    当没有配置重试策略时，SDK 使用 StandardRetryer() 作为客户端的默认实现，其默认配置如下：
    - max_attempts：设置最大尝试次数。默认为3次
    - max_backoff：设置最大退避时间（单位：秒）。默认为20秒
    - base_delay：设置基础延迟时间（单位：秒）。默认为0.2秒
    - backoff_delayer：设置退避算法。默认使用FullJitter退避算法
      计算公式为：[0.0, 1.0) * min(2^attempts * baseDelay, maxBackoff)
    - error_retryables：可重试的错误类型，请参阅https://gosspublic.alicdn.com/sdk-doc/alibabacloud-oss-python-sdk-v2/latest/_modules/alibabacloud_oss_v2/retry/error_retryable.html

    当发生可重试错误时，将使用其提供的配置来延迟并随后重试该请求。
    请求的总体延迟会随着重试次数而增加，如果默认配置不满足您的场景需求时，
    可配置重试参数或者修改重试实现。
    """

    # 从环境变量中加载凭证信息，用于身份验证
    credentials_provider = oss.credentials.EnvironmentVariableCredentialsProvider()

    # 加载默认的配置信息
    cfg = oss.config.load_default()

    # 配置重试策略示例：

    # 1. 自定义最大重试次数（默认为3次，这里设置为5次）
    cfg.retryer = oss.retry.StandardRetryer(max_attempts=5)

    # 2. 自定义退避延迟时间
    # 调整基础延迟时间 base_delay 为0.5秒（默认0.2秒），最大退避时间 max_backoff 为25秒（默认20秒）
    # cfg.retryer = oss.retry.StandardRetryer(max_backoff=25, base_delay=0.5)

    # 3. 自定义退避算法
    # 使用固定时间退避算法替代默认的FullJitter算法，每次延迟2秒
    # cfg.retryer = oss.retry.StandardRetryer(backoff_delayer=oss.retry.FixedDelayBackoff(2))

    # 4. 禁用重试策略
    # 如需禁用所有重试尝试时，可以使用retry.NopRetryer实现
    # cfg.retryer = oss.retry.NopRetryer()

    # 设置凭证提供者
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    # 使用配置好的信息创建OSS客户端
    client = oss.Client(cfg)

    # 使用创建好的client执行后续操作... 

# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数

```

--------------------------------

### Live Channel Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing live channel status, configuration, and statistics.

```APIDOC
## PUT /live-channels/<channel_name>/status

### Description
Changes the status of a live channel.

### Method
PUT

### Endpoint
`/live-channels/<channel_name>/status`

### Parameters
#### Path Parameters
- **channel_name** (string) - Required - The name of the live channel.

### Request Body
- **LiveChannelStatus** (json) - Required - The desired status.
  - **Status** (string) - Required - The status ('active' or 'inactive').

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## PUT /live-channels

### Description
Creates a live channel.

### Method
PUT

### Endpoint
`/live-channels`

### Request Body
- **LiveChannelConfiguration** (json) - Required - The configuration for the live channel.
  - **Name** (string) - Required - The name of the live channel.
  - **Description** (string) - Optional - A description for the channel.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /live-channels/<channel_name>/playlist

### Description
Retrieves the playlist for a live channel.

### Method
GET

### Endpoint
`/live-channels/<channel_name>/playlist`

### Parameters
#### Path Parameters
- **channel_name** (string) - Required - The name of the live channel.

### Response
#### Success Response (200)
- **Playlist** (string) - The playlist content (e.g., M3U8 format).

### Response Example
```
#EXTM3U
#EXT-X-VERSION:3
#EXTINF:10.0,
http://example.com/live/channel-name/segment1.ts
#EXTINF:10.0,
http://example.com/live/channel-name/segment2.ts
```
```

```APIDOC
## POST /live-channels/<channel_name>/playlist

### Description
Generates a playlist for a live channel.

### Method
POST

### Endpoint
`/live-channels/<channel_name>/playlist`

### Parameters
#### Path Parameters
- **channel_name** (string) - Required - The name of the live channel.

### Response
#### Success Response (200)
- **PlaylistURL** (string) - The URL of the generated playlist.

### Response Example
```json
{
  "PlaylistURL": "http://example.com/live/channel-name/playlist.m3u8"
}
```
```

```APIDOC
## GET /live-channels/<channel_name>/stat

### Description
Retrieves the streaming status information for a live channel.

### Method
GET

### Endpoint
`/live-channels/<channel_name>/stat`

### Parameters
#### Path Parameters
- **channel_name** (string) - Required - The name of the live channel.

### Response
#### Success Response (200)
- **LiveChannelStat** (json) - The streaming status.
  - **Status** (string) - The current streaming status ('live' or 'offline').
  - **Uptime** (integer) - The duration of the current stream in seconds.

### Response Example
```json
{
  "LiveChannelStat": {
    "Status": "live",
    "Uptime": 3600
  }
}
```
```

```APIDOC
## GET /live-channels/<channel_name>

### Description
Retrieves the configuration information for a live channel.

### Method
GET

### Endpoint
`/live-channels/<channel_name>`

### Parameters
#### Path Parameters
- **channel_name** (string) - Required - The name of the live channel.

### Response
#### Success Response (200)
- **LiveChannelInfo** (json) - The configuration information for the live channel.

### Response Example
```json
{
  "LiveChannelInfo": {
    "Name": "my-live-channel",
    "Description": "My awesome live stream",
    "CreateTime": "2023-10-27T10:00:00Z"
  }
}
```
```

```APIDOC
## GET /live-channels/<channel_name>/history

### Description
Retrieves the streaming history for a live channel.

### Method
GET

### Endpoint
`/live-channels/<channel_name>/history`

### Parameters
#### Path Parameters
- **channel_name** (string) - Required - The name of the live channel.

### Response
#### Success Response (200)
- **LiveChannelHistory** (array) - A list of historical streaming records.
  - **StartTime** (string) - The start time of the stream.
  - **EndTime** (string) - The end time of the stream.
  - **Duration** (integer) - The duration of the stream in seconds.

### Response Example
```json
[
  {
    "StartTime": "2023-10-26T09:00:00Z",
    "EndTime": "2023-10-26T10:00:00Z",
    "Duration": 3600
  }
]
```
```

```APIDOC
## LIST /live-channels

### Description
Lists all live channels.

### Method
GET

### Endpoint
`/live-channels`

### Response
#### Success Response (200)
- **LiveChannels** (array) - A list of live channels.
  - **Name** (string) - The name of the live channel.

### Response Example
```json
[
  {
    "Name": "channel-1"
  },
  {
    "Name": "channel-2"
  }
]
```
```

```APIDOC
## DELETE /live-channels/<channel_name>

### Description
Deletes a live channel.

### Method
DELETE

### Endpoint
`/live-channels/<channel_name>`

### Parameters
#### Path Parameters
- **channel_name** (string) - Required - The name of the live channel.

### Response
#### Success Response (200)
(No content)
```

--------------------------------

### Bucket Logging Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing access logging configurations for Buckets.

```APIDOC
## PutBucketLogging

### Description
Enables access logging for a Bucket.

### Method
POST

### Endpoint
/PutBucketLogging

## GetBucketLogging

### Description
Retrieves the access logging configuration for a Bucket.

### Method
GET

### Endpoint
/GetBucketLogging

## DeleteBucketLogging

### Description
Disables access logging for a Bucket.

### Method
POST

### Endpoint
/DeleteBucketLogging

## PutUserDefinedLogFieldsConfig

### Description
Configures custom user-defined fields for real-time bucket logs.

### Method
POST

### Endpoint
/PutUserDefinedLogFieldsConfig

## GetUserDefinedLogFieldsConfig

### Description
Retrieves the custom user-defined fields configuration for real-time bucket logs.

### Method
GET

### Endpoint
/GetUserDefinedLogFieldsConfig

## DeleteUserDefinedLogFieldsConfig

### Description
Deletes the custom user-defined fields configuration for real-time bucket logs.

### Method
POST

### Endpoint
/DeleteUserDefinedLogFieldsConfig
```

--------------------------------

### Vector Bucket Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing vector buckets, indexes, and vectors.

```APIDOC
## PUT /vector-buckets

### Description
Creates a vector bucket.

### Method
PUT

### Endpoint
`/vector-buckets`

### Request Body
- **VectorBucketConfiguration** (json) - Required - Configuration for the vector bucket.
  - **Name** (string) - Required - The name of the vector bucket.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /vector-buckets/<bucket_name>

### Description
Retrieves detailed information about a vector bucket.

### Method
GET

### Endpoint
`/vector-buckets/<bucket_name>`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the vector bucket.

### Response
#### Success Response (200)
- **VectorBucketInfo** (json) - Information about the vector bucket.

### Response Example
```json
{
  "VectorBucketInfo": {
    "Name": "my-vector-bucket",
    "CreationDate": "2023-10-27T10:00:00Z"
  }
}
```
```

```APIDOC
## LIST /vector-buckets

### Description
Lists all vector buckets under the current account.

### Method
GET

### Endpoint
`/vector-buckets`

### Response
#### Success Response (200)
- **VectorBuckets** (array) - A list of vector buckets.
  - **Name** (string) - The name of the vector bucket.

### Response Example
```json
{
  "VectorBuckets": [
    {
      "Name": "my-vector-bucket-1"
    },
    {
      "Name": "my-vector-bucket-2"
    }
  ]
}
```
```

```APIDOC
## DELETE /vector-buckets/<bucket_name>

### Description
Deletes a vector bucket.

### Method
DELETE

### Endpoint
`/vector-buckets/<bucket_name>`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the vector bucket.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## PUT /vector-buckets/<bucket_name>/indexes

### Description
Creates a vector index in a vector bucket.

### Method
PUT

### Endpoint
`/vector-buckets/<bucket_name>/indexes`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the vector bucket.

### Request Body
- **VectorIndexConfiguration** (json) - Required - Configuration for the vector index.
  - **Name** (string) - Required - The name of the vector index.
  - **Dimension** (integer) - Required - The dimension of the vectors.
  - **Metric** (string) - Required - The similarity metric (e.g., 'L2', 'IP').

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /vector-buckets/<bucket_name>/indexes/<index_name>

### Description
Retrieves detailed information about a vector index.

### Method
GET

### Endpoint
`/vector-buckets/<bucket_name>/indexes/<index_name>`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the vector bucket.
- **index_name** (string) - Required - The name of the vector index.

### Response
#### Success Response (200)
- **VectorIndexInfo** (json) - Information about the vector index.

### Response Example
```json
{
  "VectorIndexInfo": {
    "Name": "my-vector-index",
    "Dimension": 128,
    "Metric": "L2",
    "CreationDate": "2023-10-27T10:00:00Z"
  }
}
```
```

```APIDOC
## LIST /vector-buckets/<bucket_name>/indexes

### Description
Lists all vector indexes in a vector bucket.

### Method
GET

### Endpoint
`/vector-buckets/<bucket_name>/indexes`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the vector bucket.

### Response
#### Success Response (200)
- **VectorIndexes** (array) - A list of vector indexes.
  - **Name** (string) - The name of the vector index.

### Response Example
```json
{
  "VectorIndexes": [
    {
      "Name": "index-1"
    },
    {
      "Name": "index-2"
    }
  ]
}
```
```

```APIDOC
## DELETE /vector-buckets/<bucket_name>/indexes/<index_name>

### Description
Deletes a vector index from a vector bucket.

### Method
DELETE

### Endpoint
`/vector-buckets/<bucket_name>/indexes/<index_name>`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the vector bucket.
- **index_name** (string) - Required - The name of the vector index.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## PUT /vector-buckets/<bucket_name>/indexes/<index_name>/vectors

### Description
Writes vector data to an index.

### Method
PUT

### Endpoint
`/vector-buckets/<bucket_name>/indexes/<index_name>/vectors`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the vector bucket.
- **index_name** (string) - Required - The name of the vector index.

### Request Body
- **Vectors** (array) - Required - A list of vectors to write.
  - **ID** (string) - Required - The ID of the vector.
  - **Vector** (array) - Required - The vector data (list of floats).
  - **Metadata** (json) - Optional - Metadata associated with the vector.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /vector-buckets/<bucket_name>/indexes/<index_name>/vectors/<vector_id>

### Description
Retrieves specific vector data from an index.

### Method
GET

### Endpoint
`/vector-buckets/<bucket_name>/indexes/<index_name>/vectors/<vector_id>`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the vector bucket.
- **index_name** (string) - Required - The name of the vector index.
- **vector_id** (string) - Required - The ID of the vector to retrieve.

### Response
#### Success Response (200)
- **VectorData** (json) - The retrieved vector data.
  - **ID** (string) - The ID of the vector.
  - **Vector** (array) - The vector data.
  - **Metadata** (json) - Metadata associated with the vector.

### Response Example
```json
{
  "VectorData": {
    "ID": "vector-1",
    "Vector": [0.1, 0.2, 0.3],
    "Metadata": {
      "source": "document A"
    }
  }
}
```
```

```APIDOC
## LIST /vector-buckets/<bucket_name>/indexes/<index_name>/vectors

### Description
Lists all vector data within a vector index.

### Method
GET

### Endpoint
`/vector-buckets/<bucket_name>/indexes/<index_name>/vectors`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the vector bucket.
- **index_name** (string) - Required - The name of the vector index.

### Query Parameters
- **limit** (integer) - Optional - The maximum number of vectors to return.
- **next_token** (string) - Optional - A token for paginating results.

### Response
#### Success Response (200)
- **Vectors** (array) - A list of vector data.
  - **ID** (string) - The ID of the vector.
  - **Vector** (array) - The vector data.
  - **Metadata** (json) - Metadata associated with the vector.
- **NextToken** (string) - A token for the next page of results.

### Response Example
```json
{
  "Vectors": [
    {
      "ID": "vector-1",
      "Vector": [0.1, 0.2, 0.3],
      "Metadata": {"source": "doc A"}
    }
  ],
  "NextToken": "next-page-token"
}
```
```

```APIDOC
## DELETE /vector-buckets/<bucket_name>/indexes/<index_name>/vectors

### Description
Deletes specified vector data from a vector index.

### Method
DELETE

### Endpoint
`/vector-buckets/<bucket_name>/indexes/<index_name>/vectors`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the vector bucket.
- **index_name** (string) - Required - The name of the vector index.

### Request Body
- **VectorIDs** (array) - Required - A list of vector IDs to delete.
  - **ID** (string) - Required - The ID of the vector to delete.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## POST /vector-buckets/<bucket_name>/indexes/<index_name>/query

### Description
Performs a vector similarity search.

### Method
POST

### Endpoint
`/vector-buckets/<bucket_name>/indexes/<index_name>/query`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the vector bucket.
- **index_name** (string) - Required - The name of the vector index.

### Request Body
- **QueryVector** (array) - Required - The query vector.
- **K** (integer) - Required - The number of nearest neighbors to retrieve.
- **MetadataFilter** (json) - Optional - Filters for metadata.

### Response
#### Success Response (200)
- **Results** (array) - A list of search results.
  - **ID** (string) - The ID of the vector.
  - **Score** (float) - The similarity score.
  - **Metadata** (json) - Metadata associated with the vector.

### Response Example
```json
{
  "Results": [
    {
      "ID": "vector-abc",
      "Score": 0.95,
      "Metadata": {"source": "doc B"}
    }
  ]
}
```
```

--------------------------------

### Bucket Access Point Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for creating, managing, and configuring access points for Buckets.

```APIDOC
## CreateAccessPoint

### Description
Creates an access point.

### Method
POST

### Endpoint
/CreateAccessPoint

## GetAccessPoint

### Description
Retrieves information about an access point.

### Method
GET

### Endpoint
/GetAccessPoint

## DeleteAccessPoint

### Description
Deletes an access point.

### Method
POST

### Endpoint
/DeleteAccessPoint

## ListAccessPoints

### Description
Retrieves user-level or bucket-level access point information.

### Method
GET

### Endpoint
/ListAccessPoints

## PutAccessPointPolicy

### Description
Configures the policy for an access point.

### Method
POST

### Endpoint
/PutAccessPointPolicy

## GetAccessPointPolicy

### Description
Retrieves the policy configuration for an access point.

### Method
GET

### Endpoint
/GetAccessPointPolicy

## DeleteAccessPointPolicy

### Description
Deletes the policy configuration for an access point.

### Method
POST

### Endpoint
/DeleteAccessPointPolicy
```

--------------------------------

### Bucket Transfer Acceleration Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for configuring and managing transfer acceleration for Buckets.

```APIDOC
## PutBucketTransferAcceleration

### Description
Configures transfer acceleration for a Bucket.

### Method
POST

### Endpoint
/PutBucketTransferAcceleration

## GetBucketTransferAcceleration

### Description
Retrieves the transfer acceleration configuration for a Bucket.

### Method
GET

### Endpoint
/GetBucketTransferAcceleration
```

--------------------------------

### Bucket Access Monitor API

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for configuring and retrieving the access monitoring status for OSS buckets.

```APIDOC
## PutBucketAccessMonitor

### Description
Configures the access monitoring status for an OSS bucket.

### Method
PUT

### Endpoint
/bucketName?accessMonitor

### Request Body
- **accessMonitor** (object) - Required - Access monitor configuration.
  - **status** (string) - Required - The status of access monitoring. Possible values: `Enabled`, `Disabled`.

### Response
#### Success Response (200)
- **No content**

## GetBucketAccessMonitor

### Description
Retrieves the access monitoring status for an OSS bucket.

### Method
GET

### Endpoint
/bucketName?accessMonitor

### Response
#### Success Response (200)
- **accessMonitor** (object)
  - **status** (string) - The status of access monitoring. Possible values: `Enabled`, `Disabled`.
```

--------------------------------

### Bucket Versioning Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing versioning status and listing object versions for Buckets.

```APIDOC
## PutBucketVersioning

### Description
Sets the versioning status for a Bucket.

### Method
POST

### Endpoint
/PutBucketVersioning

## GetBucketVersioning

### Description
Retrieves the versioning status for a Bucket.

### Method
GET

### Endpoint
/GetBucketVersioning

## ListObjectVersions (GetBucketVersions)

### Description
Lists all object versions within a Bucket.

### Method
GET

### Endpoint
/ListObjectVersions (or /GetBucketVersions)
```

--------------------------------

### 构造 Callback-var 参数

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

用于传递自定义业务信息，key 必须以 x: 开头。需配合 callbackBody 中的占位符使用。

```json
{
  "x:uid": "12345",
  "x:order_id": "67890"
}
```

```json
{
  "callbackUrl": "http://oss-demo.aliyuncs.com:23450",
  "callbackBody": "uid=${x:uid}&order=${x:order_id}"
}
```

```text
uid=12345&order=67890
```

--------------------------------

### Data Indexing API

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for managing data indexing and metadata querying for OSS buckets.

```APIDOC
## OpenMetaQuery

### Description
Enables the metadata management feature for an OSS bucket.

### Method
POST

### Endpoint
/bucketName?metaQuery&enable

### Response
#### Success Response (200)
- **No content**

## GetMetaQueryStatus

### Description
Retrieves the metadata indexing information for a specified OSS bucket.

### Method
GET

### Endpoint
/bucketName?metaQuery

### Response
#### Success Response (200)
- **metaQueryStatus** (object)
  - **status** (string) - The status of the metadata query. Possible values: `Enabled`, `Disabled`.

## DoMetaQuery

### Description
Queries Objects that meet specified conditions and lists Object information according to specified fields and sorting methods.

### Method
POST

### Endpoint
/bucketName?metaQuery

### Request Body
- **metaQuery** (object) - Required - Meta query parameters.
  - **query** (string) - Required - The query condition.
  - **fields** (array) - Optional - Fields to retrieve.
  - **orderBy** (string) - Optional - Field to sort by.
  - **order** (string) - Optional - Sorting order. Possible values: `ASC`, `DESC`.

### Response
#### Success Response (200)
- **objects** (array) - List of matching objects.
  - **objectInfo** (object) - Information about an object.
    - **key** (string) - Object key.
    - **size** (integer) - Object size.
    - **lastModified** (string) - Last modified timestamp.

## CloseMetaQuery

### Description
Disables the metadata management feature for an OSS bucket.

### Method
POST

### Endpoint
/bucketName?metaQuery&disable

### Response
#### Success Response (200)
- **No content**
```

--------------------------------

### Bucket TLS Configuration Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for enabling, disabling, and retrieving TLS version settings for Buckets.

```APIDOC
## PutBucketHttpsConfig

### Description
Enables or disables TLS version settings for a Bucket.

### Method
POST

### Endpoint
/PutBucketHttpsConfig

## GetBucketHttpsConfig

### Description
Retrieves the TLS version settings for a Bucket.

### Method
GET

### Endpoint
/GetBucketHttpsConfig
```

--------------------------------

### Bucket Replication Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for configuring, managing, and monitoring data replication rules for Buckets.

```APIDOC
## PutBucketReplication

### Description
Sets data replication rules for a Bucket.

### Method
POST

### Endpoint
/PutBucketReplication

## PutBucketRTC

### Description
Enables or disables replication time control (RTC) for existing cross-region replication rules.

### Method
POST

### Endpoint
/PutBucketRTC

## GetBucketReplication

### Description
Retrieves the data replication rules configured for a Bucket.

### Method
GET

### Endpoint
/GetBucketReplication

## GetBucketReplicationLocation

### Description
Retrieves the regions of target Buckets for replication.

### Method
GET

### Endpoint
/GetBucketReplicationLocation

## GetBucketReplicationProgress

### Description
Retrieves the progress of data replication for a Bucket.

### Method
GET

### Endpoint
/GetBucketReplicationProgress

## DeleteBucketReplication

### Description
Stops data replication tasks and deletes replication configurations for a Bucket.

### Method
POST

### Endpoint
/DeleteBucketReplication
```

--------------------------------

### DeleteBucketQosInfo Request Syntax

Source: https://help.aliyun.com/zh/oss/developer-reference/api-delete-bucket-qos-info

The HTTP DELETE request structure for removing bucket QoS information.

```http
DELETE /?qosInfo HTTP/1.1
Host: BucketName.oss-cn-hangzhou.aliyuncs.com
Date: GMT Date
Authorization: SignatureValue
```

--------------------------------

### Object Operations API

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing objects within OSS, including uploading, downloading, copying, deleting, and retrieving metadata.

```APIDOC
## PUT /<key>

### Description
Uploads an object to OSS.

### Method
PUT

### Endpoint
`/<key>`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object to upload.

### Request Body
- **file** (binary) - Required - The content of the object.

### Response
#### Success Response (200)
- **ETag** (string) - The ETag of the uploaded object.
- **LastModified** (string) - The last modified date of the object.

### Request Example
(Binary data for the object)

### Response Example
```json
{
  "ETag": "etag-value",
  "LastModified": "2023-10-27T10:00:00Z"
}
```
```

```APIDOC
## GET /<key>

### Description
Retrieves an object from OSS.

### Method
GET

### Endpoint
`/<key>`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object to retrieve.

### Response
#### Success Response (200)
- **Content** (binary) - The content of the object.

### Response Example
(Binary data for the object)
```

```APIDOC
## POST /<key>?uploads

### Description
Initializes a multipart upload event.

### Method
POST

### Endpoint
`/<key>?uploads`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object to upload.

### Response
#### Success Response (200)
- **UploadId** (string) - The ID for the multipart upload.

### Response Example
```json
{
  "UploadId": "upload-id-value"
}
```
```

```APIDOC
## PUT /<key>?partNumber&uploadId

### Description
Uploads a part of a multipart upload.

### Method
PUT

### Endpoint
`/<key>?partNumber&uploadId`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object.
- **partNumber** (integer) - Required - The part number.
- **uploadId** (string) - Required - The upload ID of the multipart upload.

### Request Body
- **file** (binary) - Required - The content of the part.

### Response
#### Success Response (200)
- **ETag** (string) - The ETag of the uploaded part.

### Response Example
```json
{
  "ETag": "part-etag-value"
}
```
```

```APIDOC
## POST /<key>?uploadId

### Description
Completes a multipart upload.

### Method
POST

### Endpoint
`/<key>?uploadId`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object.
- **uploadId** (string) - Required - The upload ID of the multipart upload.

### Request Body
- **Parts** (array) - Required - A list of parts that have been uploaded.
  - **PartNumber** (integer) - Required - The part number.
  - **ETag** (string) - Required - The ETag of the part.

### Response
#### Success Response (200)
- **Location** (string) - The URL of the uploaded object.
- **Bucket** (string) - The bucket name.
- **Key** (string) - The key of the object.
- **ETag** (string) - The ETag of the completed object.

### Response Example
```json
{
  "Location": "http://example.com/bucket/key",
  "Bucket": "bucket-name",
  "Key": "key-name",
  "ETag": "completed-etag-value"
}
```
```

```APIDOC
## DELETE /<key>

### Description
Deletes a single object from OSS.

### Method
DELETE

### Endpoint
`/<key>`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object to delete.

### Response
#### Success Response (204)
(No content)
```

```APIDOC
## POST /?delete

### Description
Deletes multiple objects from OSS.

### Method
POST

### Endpoint
`/?delete`

### Request Body
- **Objects** (array) - Required - A list of objects to delete.
  - **Key** (string) - Required - The key of the object to delete.

### Response
#### Success Response (200)
- **Deleted** (array) - A list of objects that were successfully deleted.
  - **Key** (string) - The key of the deleted object.
- **Error** (array) - A list of errors encountered during deletion.
  - **Key** (string) - The key of the object that failed to delete.
  - **Code** (string) - The error code.
  - **Message** (string) - The error message.

### Response Example
```json
{
  "Deleted": [
    {
      "Key": "object1.txt"
    }
  ],
  "Error": [
    {
      "Key": "object2.txt",
      "Code": "NoSuchKey",
      "Message": "The specified key does not exist."
    }
  ]
}
```
```

```APIDOC
## HEAD /<key>

### Description
Retrieves the metadata of an object without returning its content.

### Method
HEAD

### Endpoint
`/<key>`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object.

### Response
#### Success Response (200)
- **ETag** (string) - The ETag of the object.
- **Content-Length** (integer) - The size of the object in bytes.
- **Last-Modified** (string) - The last modified date of the object.
- **Content-Type** (string) - The content type of the object.

### Response Example
```json
{
  "ETag": "etag-value",
  "Content-Length": 1024,
  "Last-Modified": "2023-10-27T10:00:00Z",
  "Content-Type": "text/plain"
}
```
```

```APIDOC
## POST /<key>?callback

### Description
Uploads an object with a callback.

### Method
POST

### Endpoint
`/<key>?callback`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object to upload.

### Request Body
- **file** (binary) - Required - The content of the object.
- **callback** (json) - Optional - Callback parameters.
  - **callbackUrl** (string) - Required - The URL to send the callback to.
  - **callbackHost** (string) - Optional - The host for the callback.
  - **callbackBody** (string) - Required - The body of the callback.

### Response
#### Success Response (200)
- **CallbackResult** (json) - The result of the callback.

### Response Example
```json
{
  "CallbackResult": {
    "status": 200,
    "body": "callback body content"
  }
}
```
```

```APIDOC
## POST /<key>?restore

### Description
Restores an object from archive or cold archive storage.

### Method
POST

### Endpoint
`/<key>?restore`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object to restore.

### Request Body
- **RestoreDays** (integer) - Required - The number of days to keep the object restored.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## POST /<key>?cleanrestore

### Description
Cleans up the restored status of an object.

### Method
POST

### Endpoint
`/<key>?cleanrestore`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## POST /<key>?select

### Description
Executes a SQL query on an object.

### Method
POST

### Endpoint
`/<key>?select`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object.

### Request Body
- **SelectParameters** (json) - Required - Parameters for the select query.
  - **Expression** (string) - Required - The SQL expression.
  - **InputSerialization** (json) - Required - Input serialization format.
    - **CSV** (json) - Optional - CSV format options.
      - **FieldDelimiter** (string) - Optional - Field delimiter.
      - **RecordDelimiter** (string) - Optional - Record delimiter.
    - **JSON** (json) - Optional - JSON format options.
    - **Parquet** (json) - Optional - Parquet format options.
  - **OutputSerialization** (json) - Required - Output serialization format.
    - **CSV** (json) - Optional - CSV format options.
    - **JSON** (json) - Optional - JSON format options.

### Response
#### Success Response (200)
- **SelectResult** (string) - The result of the select query.

### Response Example
```json
{
  "SelectResult": "query result content"
}
```
```

```APIDOC
## POST /<key>?select-meta

### Description
Retrieves metadata for select queries on an object.

### Method
POST

### Endpoint
`/<key>?select-meta`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object.

### Response
#### Success Response (200)
- **TotalLines** (integer) - The total number of lines in the object.
- **TotalColumns** (integer) - The total number of columns in the object (for CSV files).
- **Splits** (integer) - The number of splits for the object.

### Response Example
```json
{
  "TotalLines": 1000,
  "TotalColumns": 5,
  "Splits": 10
}
```
```

```APIDOC
## PUT /<key>?acl

### Description
Modifies the access control list (ACL) of an object.

### Method
PUT

### Endpoint
`/<key>?acl`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object.

### Headers
- **x-oss-object-acl** (string) - Required - The ACL to set (e.g., 'private', 'public-read', 'public-read-write').

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /<key>?acl

### Description
Retrieves the access control list (ACL) of an object.

### Method
GET

### Endpoint
`/<key>?acl`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object.

### Response
#### Success Response (200)
- **AccessControlList** (json) - The ACL configuration.

### Response Example
```json
{
  "AccessControlList": {
    "Owner": {
      "ID": "owner-id"
    },
    "Grants": [
      {
        "Grantee": {
          "Type": "Group",
          "URI": "http://acs.oss.aliyuncs.com/groups/global/authenticated"
        },
        "Permission": "READ"
      }
    ]
  }
}
```
```

```APIDOC
## PUT /<symlink_key>?symlink

### Description
Creates a symbolic link (symlink) to an object.

### Method
PUT

### Endpoint
`/<symlink_key>?symlink`

### Parameters
#### Path Parameters
- **symlink_key** (string) - Required - The key for the symbolic link.

### Headers
- **x-oss-symlink-target** (string) - Required - The target object key.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /<symlink_key>?symlink

### Description
Retrieves the target of a symbolic link.

### Method
GET

### Endpoint
`/<symlink_key>?symlink`

### Parameters
#### Path Parameters
- **symlink_key** (string) - Required - The key of the symbolic link.

### Response
#### Success Response (200)
- **SymlinkTarget** (string) - The target object key.

### Response Example
```json
{
  "SymlinkTarget": "target/object/key"
}
```
```

```APIDOC
## PUT /<key>?tagging

### Description
Sets or updates tags for an object.

### Method
PUT

### Endpoint
`/<key>?tagging`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object.

### Request Body
- **TagSet** (json) - Required - A set of tags.
  - **Tag** (array) - A list of tags.
    - **Key** (string) - Required - The tag key.
    - **Value** (string) - Required - The tag value.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /<key>?tagging

### Description
Retrieves tags for an object.

### Method
GET

### Endpoint
`/<key>?tagging`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object.

### Response
#### Success Response (200)
- **TagSet** (json) - A set of tags.
  - **Tag** (array) - A list of tags.
    - **Key** (string) - The tag key.
    - **Value** (string) - The tag value.

### Response Example
```json
{
  "TagSet": {
    "Tag": [
      {
        "Key": "environment",
        "Value": "production"
      }
    ]
  }
}
```
```

```APIDOC
## DELETE /<key>?tagging

### Description
Deletes tags for an object.

### Method
DELETE

### Endpoint
`/<key>?tagging`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object.

### Response
#### Success Response (200)
(No content)
```

--------------------------------

### Bucket Inventory Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for configuring, retrieving, and managing inventory tasks for Buckets.

```APIDOC
## PutBucketInventory

### Description
Sets an inventory task for a Bucket.

### Method
POST

### Endpoint
/PutBucketInventory

## GetBucketInventory

### Description
Retrieves a specific inventory task for a Bucket.

### Method
GET

### Endpoint
/GetBucketInventory

## ListBucketInventory

### Description
Retrieves all inventory tasks configured for a Bucket.

### Method
GET

### Endpoint
/ListBucketInventory

## DeleteBucketInventory

### Description
Deletes a specific inventory task for a Bucket.

### Method
POST

### Endpoint
/DeleteBucketInventory
```

--------------------------------

### Vector Index Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for creating, retrieving, listing, and deleting vector indexes within a vector bucket.

```APIDOC
## POST /PutVectorIndex

### Description
Creates a vector index within a vector bucket.

### Method
POST

### Endpoint
/PutVectorIndex

## GET /GetVectorIndex

### Description
Retrieves detailed information about a vector index.

### Method
GET

### Endpoint
/GetVectorIndex

## GET /ListVectorIndexes

### Description
Lists all vector indexes within a vector bucket.

### Method
GET

### Endpoint
/ListVectorIndexes

## DELETE /DeleteVectorIndex

### Description
Deletes a vector index. After all data parts are uploaded, you must call the CompleteMultipartUpload interface to complete the multipart upload of the entire file.

### Method
DELETE

### Endpoint
/DeleteVectorIndex
```

--------------------------------

### Bucket Referer Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for setting and retrieving referer rules for Buckets to prevent hotlinking.

```APIDOC
## PutBucketReferer

### Description
Sets referer rules for a Bucket.

### Method
POST

### Endpoint
/PutBucketReferer

## GetBucketReferer

### Description
Retrieves the referer rules for a Bucket.

### Method
GET

### Endpoint
/GetBucketReferer
```

--------------------------------

### PutPublicAccessBlock

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Configures global public access blocking for OSS.

```APIDOC
## PUT PutPublicAccessBlock

### Description
Enables global public access blocking for OSS.

### Method
PUT

### Endpoint
PutPublicAccessBlock
```

--------------------------------

### Bucket Requester QoS Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for managing QoS settings for specific requesters at the Bucket level.

```APIDOC
## PutBucketRequesterQoSInfo

### Description
Sets the flow control for a requester at the Bucket level.

### Method
POST

### Endpoint
/PutBucketRequesterQoSInfo

## GetBucketRequesterQoSInfo

### Description
Retrieves the flow control configuration for a specific requester at the Bucket level.

### Method
GET

### Endpoint
/GetBucketRequesterQoSInfo

## ListBucketRequesterQoSInfos

### Description
Retrieves the flow control configurations for all requesters at the Bucket level.

### Method
GET

### Endpoint
/ListBucketRequesterQoSInfos

## DeleteBucketRequesterQoSInfo

### Description
Deletes the flow control configuration for a specific requester for a Bucket.

### Method
DELETE

### Endpoint
/DeleteBucketRequesterQoSInfo
```

--------------------------------

### Bucket Policy Management

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Endpoints for managing Bucket access policies.

```APIDOC
## PUT /Bucket?policy

### Description
设置Bucket Policy。

### Method
PUT

## GET /Bucket?policy

### Description
获取Bucket Policy。

### Method
GET

## GET /Bucket?policyStatus

### Description
查看当前Bucket Policy是否允许公共访问。

### Method
GET

## DELETE /Bucket?policy

### Description
删除Bucket Policy。

### Method
DELETE
```

--------------------------------

### CleanRestoredObject Request Syntax

Source: https://help.aliyun.com/zh/oss/developer-reference/cleanrestoredobject

The standard HTTP POST request format for initiating a CleanRestoredObject operation.

```HTTP
POST /ObjectName?cleanRestoredObject HTTP/1.1
Host: examplebucket.oss-cn-hangzhou.aliyuncs.com
Date: GMT Date
Authorization: SignatureValue
```

--------------------------------

### OSS Response to Client After Callback

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

This is the response that OSS forwards to the client after successfully processing the callback from the application server. It includes the status and any content returned by your server.

```HTTP
HTTP/1.1 200 OK
Date: Mon, 14 Sep 2015 12:37:27 GMT
Content-Type: application/json
Content-Length: 9
Connection: keep-alive
ETag: "D8E8FCA2DC0F896FD7CB4CB0031BA249"
Server: AliyunOSS
x-oss-bucket-version: 1442231779
x-oss-request-id: 55F6BF87207FB30F2640C548
{"Status": "OK"}
```

--------------------------------

### Policy 中校验 Callback 参数

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

在上传策略中指定 callback 参数的校验条件。

```json
{ "expiration": "2021-12-01T12:00:00.000Z",
  "conditions": [
    {"bucket": "examplebucket" }
```

--------------------------------

### Bucket Data Indexing Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for enabling, querying, and disabling metadata management and indexing for Buckets.

```APIDOC
## OpenMetaQuery

### Description
Enables metadata management functionality for a Bucket.

### Method
POST

### Endpoint
/OpenMetaQuery

## GetMetaQueryStatus

### Description
Retrieves the metadata indexing information for a specified Bucket.

### Method
GET

### Endpoint
/GetMetaQueryStatus

## DoMetaQuery

### Description
Queries objects based on specified conditions and lists them with specified fields and sorting.

### Method
POST

### Endpoint
/DoMetaQuery

## CloseMetaQuery

### Description
Disables metadata management functionality for a Bucket.

### Method
POST

### Endpoint
/CloseMetaQuery
```

--------------------------------

### Base64 编码示例

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

上传前必须将 JSON 格式的 callback 和 callback-var 参数进行 Base64 编码。

```json
{
    "callbackUrl": "http://oss-demo.aliyuncs.com:23450",
    "callbackHost": "your.callback.com",
    "callbackBody": "bucket=${bucket}&object=${object}&uid=${x:uid}&order=${x:order_id}",
    "callbackBodyType": "application/x-www-form-urlencoded",
    "callbackSNI": false
}
```

```text

```

```json
{
  "x:uid": "12345",
  "x:order_id": "67890"
}
```

```text
eyJ4OnVpZCI6ICIxMjM0NSIsICJ4Om9yZGVyX2lkIjogIjY3ODkwIn0=
```

--------------------------------

### Calculate CRC64 with Boost

Source: https://help.aliyun.com/zh/oss/developer-reference/appendobject

Use the Boost CRC module to calculate the CRC64 checksum for data.

```cpp
typedef boost::crc_optimal<64, 0x42F0E1EBA9EA3693ULL, 0xffffffffffffffffULL, 0xffffffffffffffffULL, true, true> boost_ecma;
uint64_t do_boost_crc(const char* buffer, int length)
{
    boost_ecma crc;
    crc.process_bytes(buffer, length);
    return crc.checksum();
}
```

--------------------------------

### Resource Group API

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for managing resource group associations for OSS buckets.

```APIDOC
## PutBucketResourceGroup

### Description
Associates an OSS bucket with a resource group.

### Method
PUT

### Endpoint
/bucketName?resourceGroup

### Request Body
- **resourceGroup** (object) - Required - Resource group configuration.
  - **resourceGroupId** (string) - Required - The ID of the resource group.

### Response
#### Success Response (200)
- **No content**

## GetBucketResourceGroup

### Description
Retrieves the resource group ID associated with an OSS bucket.

### Method
GET

### Endpoint
/bucketName?resourceGroup

### Response
#### Success Response (200)
- **resourceGroup** (object)
  - **resourceGroupId** (string) - The ID of the resource group.
```

--------------------------------

### Object Tagging Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for managing tags associated with Aliyun OSS objects.

```APIDOC
## GET /GetObjectTagging

### Description
Retrieves the tags associated with an object.

### Method
GET

### Endpoint
/GetObjectTagging

## DELETE /DeleteObjectTagging

### Description
Deletes the tags associated with a specified object.

### Method
DELETE

### Endpoint
/DeleteObjectTagging
```

--------------------------------

### Bucket Access Monitor Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for configuring and retrieving the access monitoring status for Buckets.

```APIDOC
## PutBucketAccessMonitor

### Description
Configures the access monitoring status for a Bucket.

### Method
POST

### Endpoint
/PutBucketAccessMonitor

## GetBucketAccessMonitor

### Description
Retrieves the access monitoring status for a Bucket.

### Method
GET

### Endpoint
/GetBucketAccessMonitor
```

--------------------------------

### Bucket Object FC Access Point Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for creating, managing, and configuring access points for object processing functions (FC).

```APIDOC
## CreateAccessPointForObjectProcess

### Description
Creates an access point for object processing.

### Method
POST

### Endpoint
/CreateAccessPointForObjectProcess

## GetAccessPointForObjectProcess

### Description
Retrieves basic information about an access point for object processing.

### Method
GET

### Endpoint
/GetAccessPointForObjectProcess

## DeleteAccessPointForObjectProcess

### Description
Deletes an access point for object processing.

### Method
POST

### Endpoint
/DeleteAccessPointForObjectProcess

## ListAccessPointsForObjectProcess

### Description
Retrieves user-level access point information for object processing.

### Method
GET

### Endpoint
/ListAccessPointsForObjectProcess

## PutAccessPointConfigForObjectProcess

### Description
Modifies the configuration of an access point for object processing.

### Method
POST

### Endpoint
/PutAccessPointConfigForObjectProcess

## GetAccessPointConfigForObjectProcess

### Description
Retrieves the configuration information for an access point for object processing.

### Method
GET

### Endpoint
/GetAccessPointConfigForObjectProcess
```

--------------------------------

### Configure OIDC Role ARN Authentication

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

Uses OIDC role-based credentials to authenticate with OSS. Ensure environment variables for RoleArn, OidcProviderArn, and OidcTokenFilePath are set.

```python
# -*- coding: utf-8 -*-
import os
from alibabacloud_credentials.client import Client
from alibabacloud_credentials.models import Config
import alibabacloud_oss_v2 as oss

def main():
    config = Config(
        # 指定Credential类型，固定值为oidc_role_arn。
        type='oidc_role_arn',
        # RAM角色名称ARN，可以通过环境变量ALIBABA_CLOUD_ROLE_ARN设置RoleArn
        role_arn=os.environ.get('<RoleArn>'),
        # OIDC提供商ARN，可以通过环境变量ALIBABA_CLOUD_OIDC_PROVIDER_ARN设置OidcProviderArn
        oidc_provider_arn=os.environ.get('<OidcProviderArn>'),
        # OIDC Token文件路径，可以通过环境变量ALIBABA_CLOUD_OIDC_TOKEN_FILE设置OidcTokenFilePath
        oidc_token_file_path=os.environ.get('<OidcTokenFilePath>'),
        # 角色会话名称，可以通过环境变量ALIBABA_CLOUD_ROLE_SESSION_NAME设置RoleSessionName
        role_session_name='<RoleSessionName>',
        # 设置更小的权限策略，非必填。示例值：{"Statement": [{"Action": ["*"],"Effect": "Allow","Resource": ["*"]}],"Version":"1"}
        policy='<Policy>',
        # 设置角色会话有效期，单位为秒，默认值为3600秒（1小时），非必填
        role_session_expiration=3600
    )

    cred_client = Client(config)

    def get_credentials_wrapper():
        cred = cred_client.get_credential()
        return oss.credentials.Credentials(access_key_id=cred.access_key_id, access_key_secret=cred.access_key_secret, security_token=cred.security_token)

    # 创建凭证提供者，用于动态加载凭证
    credentials_provider = oss.credentials.CredentialsProviderFunc(func=get_credentials_wrapper)

    # 加载OSS SDK的默认配置
    cfg = oss.config.load_default()
    cfg.credentials_provider = credentials_provider

    # 填写Bucket所在地域
    cfg.region = '<region-id>'

    # 创建OSS客户端实例
    client = oss.Client(cfg)

    # 使用client进行后续操作...


# 当此脚本被直接运行时，调用main函数
if __name__ == "__main__":
    main()  # 脚本入口，当文件被直接运行时调用main函数
```

--------------------------------

### OSS Callback Overview

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

OSS supports automatically triggering callbacks after file uploads to notify your application server for subsequent operations. This section details the callback process.

```APIDOC
## Callback Flow Overview

OSS callback involves the following steps:

1.  **Client Uploads File (with Callback Parameters)**
    When uploading a file, the client must include the `Callback` parameter, specifying the callback server's address and content. Custom variables can be passed using the `callback-var` parameter.

2.  **OSS Stores File and Sends Callback Request**
    After successful file upload, OSS sends a POST request to the callback URL. This request includes file information (e.g., bucket, object, size, ETag) and any custom parameters.

3.  **Server Processes Callback and Returns Response**
    The server receives the callback request, optionally verifies the request signature for security, and must respond within 5 seconds. An HTTP status code of 200 indicates success; any other code is considered a callback failure. Callback failures do not affect the file upload's success and are not retried automatically.

4.  **OSS Returns Upload Result**
    Upon a successful callback response, OSS provides the final processing result to the client based on the application server's return value.
```

--------------------------------

### Bucket Lifecycle Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for configuring and managing object lifecycle rules within a Bucket.

```APIDOC
## PutBucketLifecycle

### Description
Sets lifecycle rules for objects within a Bucket.

### Method
POST

### Endpoint
/PutBucketLifecycle

## GetBucketLifecycle

### Description
Retrieves the lifecycle rules for objects within a Bucket.

### Method
GET

### Endpoint
/GetBucketLifecycle

## DeleteBucketLifecycle

### Description
Deletes lifecycle rules for objects within a Bucket.

### Method
POST

### Endpoint
/DeleteBucketLifecycle
```

--------------------------------

### AppendObject Request Syntax

Source: https://help.aliyun.com/zh/oss/developer-reference/appendobject

This is the basic syntax for making an AppendObject request. Ensure to replace placeholders with your specific bucket name, object name, and position. For OSS on Cloud Box, replace the Host with the Cloud Box endpoint.

```http
POST /ObjectName?append&position=Position HTTP/1.1
Content-Length：ContentLength
Content-Type: ContentType
Host: BucketName.oss.aliyuncs.com
Date: GMT Date
Authorization: SignatureValue
```

--------------------------------

### Calculate CRC64 with Python

Source: https://help.aliyun.com/zh/oss/developer-reference/appendobject

Use the crcmod library to calculate the CRC64 checksum.

```python
import crcmod
do_crc64 = crcmod.mkCrcFun(0x142F0E1EBA9EA3693, initCrc=0, xorOut=0xffffffffffffffff, rev=True)
print(do_crc64(b"123456789"))
```

--------------------------------

### DELETE /?qosInfo

Source: https://help.aliyun.com/zh/oss/developer-reference/api-delete-bucket-qos-info

Deletes the flow control configuration for a specified Bucket within the resource pool.

```APIDOC
## DELETE /?qosInfo

### Description
Deletes the flow control configuration for a specified Bucket within the resource pool. Note that this is currently an invitation-only feature and requires specific permissions (oss:DeleteBucketQoSInfo).

### Method
DELETE

### Endpoint
/?qosInfo

### Request Example
DELETE /?qosInfo HTTP/1.1
Host: oss-example.oss-cn-hangzhou.aliyuncs.com  
Date: Fri, 27 Dec 2024 03:21:12 GMT
Authorization: OSS4-HMAC-SHA256 Credential=LTAI********************/20250417/cn-hangzhou/oss/aliyun_v4_request,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e

### Response
#### Success Response (200)
- **x-oss-request-id** (string) - The unique identifier for the request.

#### Response Example
HTTP/1.1 200 OK
Date: Fri, 27 Dec 2024 03:21:12 GMT
Content-Length: 0
Connection: keep-alive
Server: AliyunOSS
x-oss-request-id: 534****
```

--------------------------------

### Custom Domain (CNAME) API

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for managing custom domain names (CNAMEs) for OSS buckets.

```APIDOC
## CreateCnameToken

### Description
Creates a CnameToken required for domain ownership verification when setting up custom domains.

### Method
POST

### Endpoint
/bucketName?cnameToken&create

### Response
#### Success Response (200)
- **cnameToken** (string) - The generated CnameToken.

## GetCnameToken

### Description
Retrieves a previously created CnameToken.

### Method
GET

### Endpoint
/bucketName?cnameToken

### Response
#### Success Response (200)
- **cnameToken** (string) - The CnameToken.

## PutCname

### Description
Binds a CNAME to an OSS bucket.

### Method
PUT

### Endpoint
/bucketName?cname

### Request Body
- **cname** (object) - Required - CNAME configuration.
  - **domain** (string) - Required - The custom domain name.

### Response
#### Success Response (200)
- **No content**

## ListCname

### Description
Retrieves a list of all CNAMEs bound to an OSS bucket.

### Method
GET

### Endpoint
/bucketName?cname

### Response
#### Success Response (200)
- **cnames** (array)
  - **domain** (string) - A bound custom domain name.

## DeleteCname

### Description
Deletes a bound CNAME from an OSS bucket.

### Method
DELETE

### Endpoint
/bucketName?cname

### Request Body
- **cname** (object) - Required - CNAME configuration.
  - **domain** (string) - Required - The custom domain name to delete.

### Response
#### Success Response (200)
- **No content**
```

--------------------------------

### Bucket Policy Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing Bucket Policies, which define access permissions.

```APIDOC
## PutBucketPolicy

### Description
Sets a Bucket Policy.

### Method
POST

### Endpoint
/PutBucketPolicy

## GetBucketPolicy

### Description
Retrieves the Bucket Policy.

### Method
GET

### Endpoint
/GetBucketPolicy

## GetBucketPolicyStatus

### Description
Checks if the current Bucket Policy allows public access.

### Method
GET

### Endpoint
/GetBucketPolicyStatus

## DeleteBucketPolicy

### Description
Deletes the Bucket Policy.

### Method
POST

### Endpoint
/DeleteBucketPolicy
```

--------------------------------

### Bucket Encryption Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for configuring and managing encryption rules for Buckets.

```APIDOC
## PutBucketEncryption

### Description
Configures encryption rules for a Bucket.

### Method
POST

### Endpoint
/PutBucketEncryption

## GetBucketEncryption

### Description
Retrieves the encryption rules for a Bucket.

### Method
GET

### Endpoint
/GetBucketEncryption

## DeleteBucketEncryption

### Description
Deletes the encryption rules for a Bucket.

### Method
POST

### Endpoint
/DeleteBucketEncryption
```

--------------------------------

### WriteGetObjectResponse API

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

API for customizing response data and headers for GetObject requests.

```APIDOC
## POST /<key>?response-content-type=...&response-cache-control=...

### Description
Customizes response data and headers for GetObject requests.

### Method
POST

### Endpoint
`/<key>?response-content-type=...&response-cache-control=...`

### Parameters
#### Path Parameters
- **key** (string) - Required - The key of the object.

#### Query Parameters
- **response-content-type** (string) - Optional - Specifies the Content-Type header for the response.
- **response-cache-control** (string) - Optional - Specifies the Cache-Control header for the response.
- **response-content-encoding** (string) - Optional - Specifies the Content-Encoding header for the response.
- **response-expires** (string) - Optional - Specifies the Expires header for the response.
- **response-content-language** (string) - Optional - Specifies the Content-Language header for the response.
- **response-content-disposition** (string) - Optional - Specifies the Content-Disposition header for the response.

### Request Body
- **file** (binary) - Optional - Custom response data. If not provided, the original object content is used.

### Response
#### Success Response (200)
- **Customized Response** - The response with customized headers and potentially modified content.

### Response Example
(Response headers and body as configured)
```

--------------------------------

### Bucket Group Operations within Resource Pools

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for managing Bucket Groups and their QoS configurations within resource pools.

```APIDOC
## PutBucketResourcePoolBucketGroup

### Description
Adds a Bucket within a resource pool to a BucketGroup.

### Method
POST

### Endpoint
/PutBucketResourcePoolBucketGroup

## ListResourcePoolBucketGroups

### Description
Lists the BucketGroup lists for a specified resource pool.

### Method
GET

### Endpoint
/ListResourcePoolBucketGroups

## PutResourcePoolBucketGroupQoSInfo

### Description
Configures or modifies the QoS for a BucketGroup within a resource pool.

### Method
POST

### Endpoint
/PutResourcePoolBucketGroupQoSInfo

## GetResourcePoolBucketGroupQoSInfo

### Description
Retrieves the QoS configuration for a BucketGroup within a resource pool.

### Method
GET

### Endpoint
/GetResourcePoolBucketGroupQoSInfo

## ListResourcePoolBucketGroupQoSInfos

### Description
Lists the QoS configurations for BucketGroups within a resource pool.

### Method
GET

### Endpoint
/ListResourcePoolBucketGroupQoSInfos

## DeleteResourcePoolBucketGroupQoSInfo

### Description
Deletes the QoS configuration for a BucketGroup within a resource pool.

### Method
DELETE

### Endpoint
/DeleteResourcePoolBucketGroupQoSInfo
```

--------------------------------

### Create External Table with S3A Location

Source: https://help.aliyun.com/zh/oss/developer-reference/best-practice-of-coordination-and-optimization-based-on-hadoop-oss-connector-client-and-oss-accelerator

Define a Hive external table using the s3a protocol. This allows existing S3-based table definitions to work with OSS without modification.

```sql
CREATE EXTERNAL TABLE ods_user (
    id BIGINT,
    name STRING,
    dt STRING
)
PARTITIONED BY (dt STRING)
STORED AS PARQUET
LOCATION 's3a://my-data-lake-bucket/dw/ods/user_info/';
```

--------------------------------

### OSSUTIL Command for AppendObject

Source: https://help.aliyun.com/zh/oss/developer-reference/appendobject

Information on the ossutil command corresponding to the AppendObject API.

```APIDOC
## OSSUTIL Command

For the ossutil command corresponding to the AppendObject API, please refer to `append-object`.
```

--------------------------------

### Bucket Request Payment Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for configuring request payment settings for Buckets.

```APIDOC
## PutBucketRequestPayment

### Description
Sets a Bucket to the requester pays mode.

### Method
POST

### Endpoint
/PutBucketRequestPayment

## GetBucketRequestPayment

### Description
Retrieves the requester pays mode configuration for a Bucket.

### Method
GET

### Endpoint
/GetBucketRequestPayment
```

--------------------------------

### 公钥URL示例

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

公钥URL的Base64编码示例及其解码结果。

```text
aHR0cDovL2dvc3NwdWJsaWMuYWxpY2RuLmNvbS9jYWxsYmFja19wdWJfa2V5X3YxLnBlbQ==
```

```text
http://gosspublic.alicdn.com/callback_pub_key_v1.pem
```

--------------------------------

### Vector Bucket Operations API

Source: https://help.aliyun.com/zh/oss/developer-reference/apis-for-operations-on-vector-buckets

Vector Buckets leverage existing authorization (Policy) and logging (Logging) APIs but require specific hostnames and JSON data formats for requests and responses.

```APIDOC
## Vector Bucket Operations

### Description
Vector Buckets reuse existing authorization (Policy) and logging (Logging) APIs. When interacting with these APIs for Vector Buckets, two key differences must be noted:
1. **Request Domain:** The Host header must use the dedicated domain name for the Vector Bucket.
2. **Data Format:** Both the Request Body and Response Body must be in JSON format.

All other usage, including parameters, remains consistent with existing APIs.

### Method
N/A (This section describes general API usage differences, not specific HTTP methods)

### Endpoint
N/A (This section describes general API usage differences, not specific endpoints)

### Parameters
N/A (This section describes general API usage differences, not specific parameters)

### Request Example
```json
{
  "note": "Request body format must be JSON for Vector Buckets."
}
```

### Response
#### Success Response (200)
- **field** (type) - Description

#### Response Example
```json
{
  "note": "Response body format must be JSON for Vector Buckets."
}
```
```

--------------------------------

### 获取公钥URL

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

从请求头中提取并解码公钥URL。

```python
public_key = urlopen(base64_decode(x-oss-pub-key-url头的值))
```

--------------------------------

### CloseMetaQuery Request Syntax

Source: https://help.aliyun.com/zh/oss/developer-reference/closemetaquery

This is the general syntax for the CloseMetaQuery API request. Ensure the Host header is correctly formatted with your bucket name and region.

```http
POST /?metaQuery&comp=delete HTTP/1.1
Host: BucketName.oss-cn-hangzhou.aliyuncs.com
Date: GMT Date
Authorization: SignatureValue
```

--------------------------------

### Bucket Tags Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing tags associated with Buckets.

```APIDOC
## PutBucketTags

### Description
Adds or modifies tags for a Bucket.

### Method
POST

### Endpoint
/PutBucketTags

## GetBucketTags

### Description
Retrieves the tags associated with a Bucket.

### Method
GET

### Endpoint
/GetBucketTags

## DeleteBucketTags

### Description
Deletes tags from a Bucket.

### Method
POST

### Endpoint
/DeleteBucketTags
```

--------------------------------

### Relationship with Other OSS Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/appendobject

Explains how the AppendObject operation interacts with other OSS operations like PutObject, HeadObject, and GetBucket.

```APIDOC
## Relationship with Other Operations

- **PutObject**: If you perform a PutObject operation on an existing Appendable Object, the object will be overwritten and converted to a Normal Object.
- **HeadObject**: Performing a HeadObject operation on an Appendable Object returns `x-oss-next-append-position`, `x-oss-hash-crc64ecma`, and `x-oss-object-type`. The `x-oss-object-type` for an Appendable Object is 'Appendable'.
- **GetBucket (ListObjects)**: In the response of a GetBucket request, the Type of an Appendable Object is set to 'Appendable'.
```

--------------------------------

### Bucket CNAME Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing custom domain names (CNAME) for Buckets.

```APIDOC
## CreateCnameToken

### Description
Creates a CnameToken required for domain ownership verification.

### Method
POST

### Endpoint
/CreateCnameToken

## GetCnameToken

### Description
Retrieves a previously created CnameToken.

### Method
GET

### Endpoint
/GetCnameToken

## PutCname

### Description
Binds a CNAME to a Bucket.

### Method
POST

### Endpoint
/PutCname

## ListCname

### Description
Lists all CNAMEs bound to a Bucket.

### Method
GET

### Endpoint
/ListCname

## DeleteCname

### Description
Deletes a bound CNAME from a Bucket.

### Method
POST

### Endpoint
/DeleteCname
```

--------------------------------

### Bucket Permissions (ACL) Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing Access Control List (ACL) permissions for Buckets.

```APIDOC
## PutBucketAcl

### Description
Sets the access permissions (ACL) for a Bucket.

### Method
POST

### Endpoint
/PutBucketAcl

## GetBucketAcl

### Description
Retrieves the access permissions (ACL) for a Bucket.

### Method
GET

### Endpoint
/GetBucketAcl
```

--------------------------------

### AbortMultipartUpload Request Syntax

Source: https://help.aliyun.com/zh/oss/developer-reference/abortmultipartupload

The HTTP request structure for canceling a multipart upload event.

```plaintext
DELETE /ObjectName?uploadId=UploadId HTTP/1.1
Host: BucketName.oss-cn-hangzhou.aliyuncs.com
Date: GMT Date
Authorization: Signature
```

--------------------------------

### Bucket Resource Group Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for associating Buckets with resource groups.

```APIDOC
## PutBucketResourceGroup

### Description
Configures the resource group to which a Bucket belongs.

### Method
POST

### Endpoint
/PutBucketResourceGroup

## GetBucketResourceGroup

### Description
Retrieves the resource group ID of a Bucket.

### Method
GET

### Endpoint
/GetBucketResourceGroup
```

--------------------------------

### CRC64 Calculation Methods

Source: https://help.aliyun.com/zh/oss/developer-reference/appendobject

Provides methods for calculating CRC64 checksums using Boost CRC module and Python's crcmod library.

```APIDOC
## CRC64 Calculation

### Boost CRC Module
```cpp
typedef boost::crc_optimal<64, 0x42F0E1EBA9EA3693ULL, 0xffffffffffffffffULL, 0xffffffffffffffffULL, true, true> boost_ecma;
uint64_t do_boost_crc(const char* buffer, int length)
{
    boost_ecma crc;
    crc.process_bytes(buffer, length);
    return crc.checksum();
}
```

### Python crcmod
```python
import crcmod
do_crc64 = crcmod.mkCrcFun(0x142F0E1EBA9EA3693, initCrc=0, xorOut=0xffffffffffffffff, rev=True)
print(do_crc64(b"123456789"))
```
```

--------------------------------

### Bucket WORM (Compliance) Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing compliance retention (WORM) policies on Buckets.

```APIDOC
## InitiateBucketWorm

### Description
Initiates a compliance retention (WORM) policy for a Bucket.

### Method
POST

### Endpoint
/InitiateBucketWorm

## AbortBucketWorm

### Description
Aborts an unlocked compliance retention (WORM) policy for a Bucket.

### Method
POST

### Endpoint
/AbortBucketWorm

## CompleteBucketWorm

### Description
Locks a compliance retention (WORM) policy for a Bucket.

### Method
POST

### Endpoint
/CompleteBucketWorm

## ExtendBucketWorm

### Description
Extends the retention period for objects within a locked WORM-enabled Bucket.

### Method
POST

### Endpoint
/ExtendBucketWorm

## GetBucketWorm

### Description
Retrieves the compliance retention (WORM) policy information for a Bucket.

### Method
GET

### Endpoint
/GetBucketWorm
```

--------------------------------

### Archive Direct Read Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for enabling and disabling archive direct read for buckets.

```APIDOC
## PUT /<bucket_name>/archive-direct-read

### Description
Enables or disables archive direct read for a bucket.

### Method
PUT

### Endpoint
`/<bucket_name>/archive-direct-read`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Request Body
- **ArchiveDirectReadConfiguration** (json) - Required - Configuration for archive direct read.
  - **Enabled** (boolean) - Required - Whether to enable archive direct read.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /<bucket_name>/archive-direct-read

### Description
Checks if archive direct read is enabled for a bucket.

### Method
GET

### Endpoint
`/<bucket_name>/archive-direct-read`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Response
#### Success Response (200)
- **ArchiveDirectReadConfiguration** (json) - The archive direct read configuration.
  - **Enabled** (boolean) - Whether archive direct read is enabled.

### Response Example
```json
{
  "ArchiveDirectReadConfiguration": {
    "Enabled": true
  }
}
```
```

--------------------------------

### 上传结果输出示例

Source: https://help.aliyun.com/zh/oss/developer-reference/2-0-manual-preview-version

上传操作成功后的标准输出格式。

```text
status code: 200
request id: 6875F95738B0ED3030F086A0
etag: "56AAD346F0899BFE8BDD02C06BBE511E"
```

--------------------------------

### Public Access Block Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for configuring and managing public access blocking for OSS resources.

```APIDOC
## PUT /public-access-block

### Description
Enables blocking of public access globally for OSS.

### Method
PUT

### Endpoint
`/public-access-block`

### Request Body
- **PublicAccessBlockConfiguration** (json) - Required - Configuration for blocking public access.
  - **BlockPublicAcls** (boolean) - Optional - Whether to block public ACLs.
  - **IgnorePublicAcls** (boolean) - Optional - Whether to ignore public ACLs.
  - **BlockPublicPolicy** (boolean) - Optional - Whether to block public policies.
  - **RestrictPublicBuckets** (boolean) - Optional - Whether to restrict public buckets.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /public-access-block

### Description
Retrieves the global public access blocking configuration for OSS.

### Method
GET

### Endpoint
`/public-access-block`

### Response
#### Success Response (200)
- **PublicAccessBlockConfiguration** (json) - The public access block configuration.

### Response Example
```json
{
  "PublicAccessBlockConfiguration": {
    "BlockPublicAcls": true,
    "IgnorePublicAcls": true,
    "BlockPublicPolicy": true,
    "RestrictPublicBuckets": true
  }
}
```
```

```APIDOC
## DELETE /public-access-block

### Description
Deletes the global public access blocking configuration for OSS.

### Method
DELETE

### Endpoint
`/public-access-block`

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## PUT /<bucket_name>/public-access-block

### Description
Enables blocking of public access for a specific bucket.

### Method
PUT

### Endpoint
`/<bucket_name>/public-access-block`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Request Body
- **PublicAccessBlockConfiguration** (json) - Required - Configuration for blocking public access.
  - **BlockPublicAcls** (boolean) - Optional - Whether to block public ACLs.
  - **IgnorePublicAcls** (boolean) - Optional - Whether to ignore public ACLs.
  - **BlockPublicPolicy** (boolean) - Optional - Whether to block public policies.
  - **RestrictPublicBuckets** (boolean) - Optional - Whether to restrict public buckets.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /<bucket_name>/public-access-block

### Description
Retrieves the public access blocking configuration for a specific bucket.

### Method
GET

### Endpoint
`/<bucket_name>/public-access-block`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Response
#### Success Response (200)
- **PublicAccessBlockConfiguration** (json) - The public access block configuration.

### Response Example
```json
{
  "PublicAccessBlockConfiguration": {
    "BlockPublicAcls": true,
    "IgnorePublicAcls": true,
    "BlockPublicPolicy": true,
    "RestrictPublicBuckets": true
  }
}
```
```

```APIDOC
## DELETE /<bucket_name>/public-access-block

### Description
Deletes the public access blocking configuration for a specific bucket.

### Method
DELETE

### Endpoint
`/<bucket_name>/public-access-block`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## PUT /accesspoint/<accesspoint_name>/public-access-block

### Description
Enables blocking of public access for an access point.

### Method
PUT

### Endpoint
`/accesspoint/<accesspoint_name>/public-access-block`

### Parameters
#### Path Parameters
- **accesspoint_name** (string) - Required - The name of the access point.

### Request Body
- **PublicAccessBlockConfiguration** (json) - Required - Configuration for blocking public access.
  - **BlockPublicAcls** (boolean) - Optional - Whether to block public ACLs.
  - **IgnorePublicAcls** (boolean) - Optional - Whether to ignore public ACLs.
  - **BlockPublicPolicy** (boolean) - Optional - Whether to block public policies.
  - **RestrictPublicBuckets** (boolean) - Optional - Whether to restrict public buckets.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /accesspoint/<accesspoint_name>/public-access-block

### Description
Retrieves the public access blocking configuration for an access point.

### Method
GET

### Endpoint
`/accesspoint/<accesspoint_name>/public-access-block`

### Parameters
#### Path Parameters
- **accesspoint_name** (string) - Required - The name of the access point.

### Response
#### Success Response (200)
- **PublicAccessBlockConfiguration** (json) - The public access block configuration.

### Response Example
```json
{
  "PublicAccessBlockConfiguration": {
    "BlockPublicAcls": true,
    "IgnorePublicAcls": true,
    "BlockPublicPolicy": true,
    "RestrictPublicBuckets": true
  }
}
```
```

```APIDOC
## DELETE /accesspoint/<accesspoint_name>/public-access-block

### Description
Deletes the public access blocking configuration for an access point.

### Method
DELETE

### Endpoint
`/accesspoint/<accesspoint_name>/public-access-block`

### Parameters
#### Path Parameters
- **accesspoint_name** (string) - Required - The name of the access point.

### Response
#### Success Response (200)
(No content)
```

--------------------------------

### Resource Group QoS Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing Quality of Service (QoS) settings for resource groups and buckets.

```APIDOC
## PUT /buckets/<bucket_name>/qos-info

### Description
Sets flow control for a bucket within a resource group.

### Method
PUT

### Endpoint
`/buckets/<bucket_name>/qos-info`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Request Body
- **BucketQoSInfo** (json) - Required - The QoS configuration for the bucket.
  - **QoSInfo** (json) - Required - QoS details.
    - **Bandwidth** (integer) - Optional - Bandwidth limit in Mbps.
    - **QPS** (integer) - Optional - Queries per second limit.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /buckets/<bucket_name>/qos-info

### Description
Retrieves the flow control configuration for a bucket within a resource group.

### Method
GET

### Endpoint
`/buckets/<bucket_name>/qos-info`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Response
#### Success Response (200)
- **BucketQoSInfo** (json) - The QoS configuration for the bucket.

### Response Example
```json
{
  "BucketQoSInfo": {
    "QoSInfo": {
      "Bandwidth": 100,
      "QPS": 1000
    }
  }
}
```
```

```APIDOC
## DELETE /buckets/<bucket_name>/qos-info

### Description
Deletes the flow control configuration for a bucket within a resource group.

### Method
DELETE

### Endpoint
`/buckets/<bucket_name>/qos-info`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## PUT /buckets/<bucket_name>/requester-qos-info

### Description
Sets requester-level flow control at the bucket level.

### Method
PUT

### Endpoint
`/buckets/<bucket_name>/requester-qos-info`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Request Body
- **RequesterQoSInfo** (json) - Required - The requester QoS configuration.
  - **RequesterID** (string) - Required - The ID of the requester.
  - **QoSInfo** (json) - Required - QoS details.
    - **Bandwidth** (integer) - Optional - Bandwidth limit in Mbps.
    - **QPS** (integer) - Optional - Queries per second limit.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /buckets/<bucket_name>/requester-qos-info

### Description
Retrieves requester-level flow control configuration for a bucket.

### Method
GET

### Endpoint
`/buckets/<bucket_name>/requester-qos-info`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Query Parameters
- **requester_id** (string) - Required - The ID of the requester.

### Response
#### Success Response (200)
- **RequesterQoSInfo** (json) - The requester QoS configuration.

### Response Example
```json
{
  "RequesterQoSInfo": {
    "RequesterID": "requester-123",
    "QoSInfo": {
      "Bandwidth": 50,
      "QPS": 500
    }
  }
}
```
```

```APIDOC
## LIST /buckets/<bucket_name>/requester-qos-infos

### Description
Lists all requester-level flow control configurations for a bucket.

### Method
GET

### Endpoint
`/buckets/<bucket_name>/requester-qos-infos`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Response
#### Success Response (200)
- **RequesterQoSInfos** (array) - A list of requester QoS configurations.

### Response Example
```json
[
  {
    "RequesterID": "requester-123",
    "QoSInfo": {"Bandwidth": 50, "QPS": 500}
  }
]
```
```

```APIDOC
## DELETE /buckets/<bucket_name>/requester-qos-info

### Description
Deletes requester-level flow control configuration for a bucket.

### Method
DELETE

### Endpoint
`/buckets/<bucket_name>/requester-qos-info`

### Parameters
#### Path Parameters
- **bucket_name** (string) - Required - The name of the bucket.

### Query Parameters
- **requester_id** (string) - Required - The ID of the requester.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## LIST /resource-pools

### Description
Lists all resource pools under the current account.

### Method
GET

### Endpoint
`/resource-pools`

### Response
#### Success Response (200)
- **ResourcePools** (array) - A list of resource pools.
  - **ResourcePoolID** (string) - The ID of the resource pool.
  - **Name** (string) - The name of the resource pool.

### Response Example
```json
[
  {
    "ResourcePoolID": "rp-abc",
    "Name": "Default Resource Pool"
  }
]
```
```

```APIDOC
## GET /resource-pools/<resource_pool_id>

### Description
Retrieves the flow control configuration for a specified resource pool.

### Method
GET

### Endpoint
`/resource-pools/<resource_pool_id>`

### Parameters
#### Path Parameters
- **resource_pool_id** (string) - Required - The ID of the resource pool.

### Response
#### Success Response (200)
- **ResourcePoolInfo** (json) - Information about the resource pool.

### Response Example
```json
{
  "ResourcePoolInfo": {
    "ResourcePoolID": "rp-abc",
    "Name": "Default Resource Pool",
    "QoSInfo": {
      "Bandwidth": 1000,
      "QPS": 10000
    }
  }
}
```
```

```APIDOC
## LIST /resource-pools/<resource_pool_id>/buckets

### Description
Lists the buckets included in a specified resource pool.

### Method
GET

### Endpoint
`/resource-pools/<resource_pool_id>/buckets`

### Parameters
#### Path Parameters
- **resource_pool_id** (string) - Required - The ID of the resource pool.

### Response
#### Success Response (200)
- **Buckets** (array) - A list of bucket names.

### Response Example
```json
[
  "bucket-1",
  "bucket-2"
]
```
```

```APIDOC
## PUT /resource-pools/<resource_pool_id>/requester-qos-info

### Description
Configures flow control for a requester within a resource pool.

### Method
PUT

### Endpoint
`/resource-pools/<resource_pool_id>/requester-qos-info`

### Parameters
#### Path Parameters
- **resource_pool_id** (string) - Required - The ID of the resource pool.

### Request Body
- **RequesterQoSInfo** (json) - Required - The requester QoS configuration.
  - **RequesterID** (string) - Required - The ID of the requester.
  - **QoSInfo** (json) - Required - QoS details.
    - **Bandwidth** (integer) - Optional - Bandwidth limit in Mbps.
    - **QPS** (integer) - Optional - Queries per second limit.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /resource-pools/<resource_pool_id>/requester-qos-info

### Description
Retrieves the flow control configuration for a requester within a resource pool.

### Method
GET

### Endpoint
`/resource-pools/<resource_pool_id>/requester-qos-info`

### Parameters
#### Path Parameters
- **resource_pool_id** (string) - Required - The ID of the resource pool.

### Query Parameters
- **requester_id** (string) - Required - The ID of the requester.

### Response
#### Success Response (200)
- **RequesterQoSInfo** (json) - The requester QoS configuration.

### Response Example
```json
{
  "RequesterQoSInfo": {
    "RequesterID": "requester-xyz",
    "QoSInfo": {
      "Bandwidth": 75,
      "QPS": 750
    }
  }
}
```
```

```APIDOC
## LIST /resource-pools/<resource_pool_id>/requester-qos-infos

### Description
Lists all requester flow control configurations for a resource pool.

### Method
GET

### Endpoint
`/resource-pools/<resource_pool_id>/requester-qos-infos`

### Parameters
#### Path Parameters
- **resource_pool_id** (string) - Required - The ID of the resource pool.

### Response
#### Success Response (200)
- **RequesterQoSInfos** (array) - A list of requester QoS configurations.

### Response Example
```json
[
  {
    "RequesterID": "requester-xyz",
    "QoSInfo": {"Bandwidth": 75, "QPS": 750}
  }
]
```
```

```APIDOC
## DELETE /resource-pools/<resource_pool_id>/requester-qos-info

### Description
Deletes the flow control configuration for a requester within a resource pool.

### Method
DELETE

### Endpoint
`/resource-pools/<resource_pool_id>/requester-qos-info`

### Parameters
#### Path Parameters
- **resource_pool_id** (string) - Required - The ID of the resource pool.

### Query Parameters
- **requester_id** (string) - Required - The ID of the requester.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## PUT /resource-pools/<resource_pool_id>/bucket-groups

### Description
Adds a bucket to a bucket group within a resource pool.

### Method
PUT

### Endpoint
`/resource-pools/<resource_pool_id>/bucket-groups`

### Parameters
#### Path Parameters
- **resource_pool_id** (string) - Required - The ID of the resource pool.

### Request Body
- **BucketGroupConfiguration** (json) - Required - Bucket group configuration.
  - **BucketName** (string) - Required - The name of the bucket.
  - **BucketGroupName** (string) - Required - The name of the bucket group.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## LIST /resource-pools/<resource_pool_id>/bucket-groups

### Description
Lists the bucket groups within a resource pool.

### Method
GET

### Endpoint
`/resource-pools/<resource_pool_id>/bucket-groups`

### Parameters
#### Path Parameters
- **resource_pool_id** (string) - Required - The ID of the resource pool.

### Response
#### Success Response (200)
- **BucketGroups** (array) - A list of bucket group names.

### Response Example
```json
[
  "group-a",
  "group-b"
]
```
```

```APIDOC
## PUT /resource-pools/<resource_pool_id>/bucket-groups/<bucket_group_name>/qos-info

### Description
Configures or modifies the QoS for a bucket group within a resource pool.

### Method
PUT

### Endpoint
`/resource-pools/<resource_pool_id>/bucket-groups/<bucket_group_name>/qos-info`

### Parameters
#### Path Parameters
- **resource_pool_id** (string) - Required - The ID of the resource pool.
- **bucket_group_name** (string) - Required - The name of the bucket group.

### Request Body
- **BucketGroupQoSInfo** (json) - Required - The QoS configuration for the bucket group.
  - **QoSInfo** (json) - Required - QoS details.
    - **Bandwidth** (integer) - Optional - Bandwidth limit in Mbps.
    - **QPS** (integer) - Optional - Queries per second limit.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /resource-pools/<resource_pool_id>/bucket-groups/<bucket_group_name>/qos-info

### Description
Retrieves the QoS configuration for a bucket group within a resource pool.

### Method
GET

### Endpoint
`/resource-pools/<resource_pool_id>/bucket-groups/<bucket_group_name>/qos-info`

### Parameters
#### Path Parameters
- **resource_pool_id** (string) - Required - The ID of the resource pool.
- **bucket_group_name** (string) - Required - The name of the bucket group.

### Response
#### Success Response (200)
- **BucketGroupQoSInfo** (json) - The QoS configuration for the bucket group.

### Response Example
```json
{
  "BucketGroupQoSInfo": {
    "QoSInfo": {
      "Bandwidth": 200,
      "QPS": 2000
    }
  }
}
```
```

```APIDOC
## LIST /resource-pools/<resource_pool_id>/bucket-groups/<bucket_group_name>/qos-infos

### Description
Lists the QoS configurations for bucket groups within a resource pool.

### Method
GET

### Endpoint
`/resource-pools/<resource_pool_id>/bucket-groups/<bucket_group_name>/qos-infos`

### Parameters
#### Path Parameters
- **resource_pool_id** (string) - Required - The ID of the resource pool.
- **bucket_group_name** (string) - Required - The name of the bucket group.

### Response
#### Success Response (200)
- **BucketGroupQoSInfos** (array) - A list of bucket group QoS configurations.

### Response Example
```json
[
  {
    "BucketGroupName": "group-a",
    "QoSInfo": {"Bandwidth": 200, "QPS": 2000}
  }
]
```
```

```APIDOC
## DELETE /resource-pools/<resource_pool_id>/bucket-groups/<bucket_group_name>/qos-info

### Description
Deletes the QoS configuration for a bucket group within a resource pool.

### Method
DELETE

### Endpoint
`/resource-pools/<resource_pool_id>/bucket-groups/<bucket_group_name>/qos-info`

### Parameters
#### Path Parameters
- **resource_pool_id** (string) - Required - The ID of the resource pool.
- **bucket_group_name** (string) - Required - The name of the bucket group.

### Response
#### Success Response (200)
(No content)
```

--------------------------------

### Debugging Callback Response Body Format

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

Use curl to send a POST request to your callback server and inspect the response body format. This helps in diagnosing issues like 'Response body is not valid json format'.

```bash
curl -d "<Content>" <CallbackServerURL> -v
```

--------------------------------

### Vector Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for writing, retrieving, listing, deleting, and querying vector data.

```APIDOC
## POST /PutVectors

### Description
Writes vector data into an index.

### Method
POST

### Endpoint
/PutVectors

## GET /GetVectors

### Description
Retrieves specified vector data.

### Method
GET

### Endpoint
/GetVectors

## GET /ListVectors

### Description
Lists all vector data within a vector index.

### Method
GET

### Endpoint
/ListVectors

## DELETE /DeleteVectors

### Description
Deletes specified vector data within a vector index.

### Method
DELETE

### Endpoint
/DeleteVectors

## POST /QueryVectors

### Description
Performs vector similarity search.

### Method
POST

### Endpoint
/QueryVectors
```

--------------------------------

### Bucket CORS API

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for managing Cross-Origin Resource Sharing (CORS) rules for OSS buckets.

```APIDOC
## PUT Bucket CORS

### Description
Sets the Cross-Origin Resource Sharing (CORS) rules for a specified OSS bucket.

### Method
PUT

### Endpoint
/bucketName?cors

### Request Body
- **cors** (object) - Required - CORS configuration.
  - **corsRule** (array) - Required - A list of CORS rules.
    - **allowedOrigin** (array) - Required - Specifies the allowed origins.
    - **allowedMethod** (array) - Required - Specifies the allowed HTTP methods.
    - **allowedHeader** (array) - Optional - Specifies the allowed request headers.
    - **exposeHeader** (array) - Optional - Specifies the response headers that can be exposed.
    - **maxAgeSeconds** (integer) - Optional - Specifies the maximum time in seconds that the browser can cache the preflight response.

### Response
#### Success Response (200)
- **No content**

## GetBucketCors

### Description
Retrieves the current Cross-Origin Resource Sharing (CORS) rules for a specified OSS bucket.

### Method
GET

### Endpoint
/bucketName?cors

### Response
#### Success Response (200)
- **cors** (object) - CORS configuration.
  - **corsRule** (array) - A list of CORS rules.
    - **allowedOrigin** (array) - Allowed origins.
    - **allowedMethod** (array) - Allowed HTTP methods.
    - **allowedHeader** (array) - Allowed request headers.
    - **exposeHeader** (array) - Response headers that can be exposed.
    - **maxAgeSeconds** (integer) - Maximum cache time for preflight response.

## DeleteBucketCors

### Description
Disables the Cross-Origin Resource Sharing (CORS) functionality for a specified OSS bucket and clears all its rules.

### Method
DELETE

### Endpoint
/bucketName?cors

### Response
#### Success Response (200)
- **No content**

## Options

### Description
Handles preflight requests (OPTIONS) sent by browsers before making cross-origin requests to OSS. It determines whether the actual request should be sent based on specific origin, HTTP method, and headers.

### Method
OPTIONS

### Endpoint
/bucketName

### Request Headers
- **Origin** (string) - Required - The origin of the request.
- **Access-Control-Request-Method** (string) - Required - The HTTP method of the actual request.
- **Access-Control-Request-Headers** (string) - Optional - The request headers of the actual request.

### Response
#### Success Response (200)
- **Access-Control-Allow-Origin** (string) - The allowed origin.
- **Access-Control-Allow-Methods** (string) - The allowed HTTP methods.
- **Access-Control-Allow-Headers** (string) - The allowed request headers.
- **Access-Control-Max-Age** (string) - The maximum cache time for the preflight response.
- **Access-Control-Expose-Headers** (string) - Response headers that can be exposed.
```

--------------------------------

### Access Point Policy Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing access policies for Object FC access points.

```APIDOC
## PUT /accesspoint/policy/object-process

### Description
Configures the permission policy for an Object FC access point.

### Method
PUT

### Endpoint
`/accesspoint/policy/object-process`

### Request Body
- **Policy** (json) - Required - The access policy configuration.

### Response
#### Success Response (200)
(No content)
```

```APIDOC
## GET /accesspoint/policy/object-process

### Description
Retrieves the permission policy configuration for an Object FC access point.

### Method
GET

### Endpoint
`/accesspoint/policy/object-process`

### Response
#### Success Response (200)
- **Policy** (json) - The access policy configuration.

### Response Example
```json
{
  "Policy": {
    "Version": "1",
    "Statement": [
      {
        "Effect": "Allow",
        "Principal": {
          "Service": "fc.aliyuncs.com"
        },
        "Action": "oss:*"
      }
    ]
  }
}
```
```

```APIDOC
## DELETE /accesspoint/policy/object-process

### Description
Deletes the permission policy configuration for an Object FC access point.

### Method
DELETE

### Endpoint
`/accesspoint/policy/object-process`

### Response
#### Success Response (200)
(No content)
```

--------------------------------

### POST /ObjectName?cleanRestoredObject

Source: https://help.aliyun.com/zh/oss/developer-reference/cleanrestoredobject

Initiates a request to clean the restored state of an object. If successful, the object returns to a frozen state and cannot be directly read.

```APIDOC
## POST /ObjectName?cleanRestoredObject

### Description
Terminates the restored state of a cold archive or deep cold archive object to stop incurring temporary storage fees for the object copy.

### Method
POST

### Endpoint
/ObjectName?cleanRestoredObject

### Request Example
POST /ObjectName?cleanRestoredObject HTTP/1.1
Host: examplebucket.oss-cn-hangzhou.aliyuncs.com
Date: Fri, 20 Sep 2024 08:32:21 GMT
Authorization: OSS4-HMAC-SHA256 Credential=LTAI****/20250417/cn-hangzhou/oss/aliyun_v4_request,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e

### Response
#### Success Response (200)
- **Status** (integer) - 200 OK, indicates the restored state has been successfully cleaned.

#### Response Example
HTTP/1.1 200 Ok
Date: Fri, 20 Sep 2024 08:32:21 GMT
Content-Length: 0
Connection: keep-alive
Server: AliyunOSS
x-oss-request-id: 5374A2880232A65C2300****
```

--------------------------------

### 回调请求示例

Source: https://help.aliyun.com/zh/oss/developer-reference/callback

包含签名信息的HTTP POST请求示例。

```http
POST /index.php?id=1&index=2 HTTP/1.0
Host: 172.16.XX.XX
Connection: close
Content-Length: 18
authorization: kKQeGTRccDKyHB3H9vF+xYMSrmhMZj****/kdD1ktNVgbWEfYTQG0G2SU/RaHBovRCE8OkQDjC3uG33esH2t****
Content-Type: application/x-www-form-urlencoded
User-Agent: http-client/0.0.1
x-oss-pub-key-url: aHR0cDovL2dvc3NwdWJsaWMuYWxpY2RuLmNvbS9jYWxsYmFja19wdWJfa2V5X3YxLnsr****
bucket=examplebucket
```

--------------------------------

### Bucket DDoS Protection Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing DDoS protection configurations for OSS instances and Buckets.

```APIDOC
## InitUserAntiDDosInfo

### Description
Creates a high-defense OSS instance.

### Method
POST

### Endpoint
/InitUserAntiDDosInfo

## UpdateUserAntiDDosInfo

### Description
Changes the status of a high-defense OSS instance.

### Method
POST

### Endpoint
/UpdateUserAntiDDosInfo

## GetUserAntiDDosInfo

### Description
Queries high-defense OSS instance information under a specified account.

### Method
GET

### Endpoint
/GetUserAntiDDosInfo

## InitBucketAntiDDosInfo

### Description
Initializes protection for a Bucket.

### Method
POST

### Endpoint
/InitBucketAntiDDosInfo

## UpdateBucketAntiDDosInfo

### Description
Updates the protection status for a Bucket.

### Method
POST

### Endpoint
/UpdateBucketAntiDDosInfo

## ListBucketAntiDDosInfo

### Description
Retrieves a list of protection information for Buckets.

### Method
GET

### Endpoint
/ListBucketAntiDDosInfo
```

--------------------------------

### Resource Pool Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for managing resource pools, including retrieving pool information, and managing requester QoS within resource pools.

```APIDOC
## ListResourcePools

### Description
Retrieves information for all resource pools under the current account.

### Method
GET

### Endpoint
/ListResourcePools

## GetResourcePoolInfo

### Description
Retrieves the QoS configuration for a specified resource pool.

### Method
GET

### Endpoint
/GetResourcePoolInfo

## ListResourcePoolBuckets

### Description
Retrieves the list of Buckets included in a specified resource pool.

### Method
GET

### Endpoint
/ListResourcePoolBuckets

## PutResourcePoolRequesterQoSInfo

### Description
Configures QoS for a requester within a resource pool.

### Method
POST

### Endpoint
/PutResourcePoolRequesterQoSInfo

## GetResourcePoolRequesterQoSInfo

### Description
Retrieves the QoS configuration for a specific requester within a resource pool.

### Method
GET

### Endpoint
/GetResourcePoolRequesterQoSInfo

## ListResourcePoolRequesterQoSInfos

### Description
Retrieves the QoS configurations for all requesters within a resource pool.

### Method
GET

### Endpoint
/ListResourcePoolRequesterQoSInfos

## DeleteResourcePoolRequesterQoSInfo

### Description
Deletes the QoS configuration for a specific requester within a resource pool.

### Method
DELETE

### Endpoint
/DeleteResourcePoolRequesterQoSInfo
```

--------------------------------

### AbortBucketWorm API

Source: https://help.aliyun.com/zh/oss/developer-reference/abortbucketworm

The AbortBucketWorm API is used to delete a compliance retention policy that has not been locked. It is important to note the conditions under which a policy can be deleted, especially within the 24-hour InProgress state.

```APIDOC
## DELETE /?worm

### Description
Deletes a non-locked compliance retention policy.

### Method
DELETE

### Endpoint
`/?worm`

### Parameters
This endpoint does not have explicit path, query, or request body parameters documented.

### Request Example
```http
DELETE /?worm HTTP/1.1
Date: Mon, 3 Aug 2020 03:15:40 GMT
Host: BucketName.oss.aliyuncs.com
Authorization: OSS4-HMAC-SHA256 Credential=LTAI********************/20250417/cn-hangzhou/oss/aliyun_v4_request,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e
```

### Response
#### Success Response (204)
- **No Content**: Indicates the policy was successfully deleted.

#### Response Example
```http
HTTP/1.1 204 NoContent
Server: AliyunOSS
Date: Tue, 22 Aug 2023 09:10:20 GMT
Content-Length: 0
Connection: keep-alive
x-oss-request-id: 5C06A3B67B8B5A3DA422299D
x-oss-server-time: 130
```
```

--------------------------------

### Bucket Access Control (ACL) Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for managing Access Control Lists (ACL) for buckets.

```APIDOC
## PutBucketAcl

### Description
Sets the access permissions for a Bucket.

### Method
PUT

### Endpoint
/bucketName?acl

## GetBucketAcl

### Description
Retrieves the access permissions for a Bucket.

### Method
GET

### Endpoint
/bucketName?acl
```

--------------------------------

### DDoS Protection API

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

APIs for managing DDoS protection configurations for user accounts and OSS buckets.

```APIDOC
## InitUserAntiDDosInfo

### Description
Creates a DDoS-protected OSS instance for a user account.

### Method
POST

### Endpoint
/user?antiDDosInfo&init

### Response
#### Success Response (200)
- **No content**

## UpdateUserAntiDDosInfo

### Description
Modifies the status of a DDoS-protected OSS instance for a user account.

### Method
PUT

### Endpoint
/user?antiDDosInfo

### Request Body
- **userAntiDDosInfo** (object) - Required - User DDoS protection configuration.
  - **status** (string) - Required - The status of DDoS protection. Possible values: `Enabled`, `Disabled`.

### Response
#### Success Response (200)
- **No content**

## GetUserAntiDDosInfo

### Description
Retrieves DDoS-protected OSS instance information for a specified user account.

### Method
GET

### Endpoint
/user?antiDDosInfo

### Response
#### Success Response (200)
- **userAntiDDosInfo** (object)
  - **status** (string) - The status of DDoS protection. Possible values: `Enabled`, `Disabled`.

## InitBucketAntiDDosInfo

### Description
Initializes DDoS protection for an OSS bucket.

### Method
POST

### Endpoint
/bucketName?antiDDosInfo&init

### Response
#### Success Response (200)
- **No content**

## UpdateBucketAntiDDosInfo

### Description
Updates the DDoS protection status for an OSS bucket.

### Method
PUT

### Endpoint
/bucketName?antiDDosInfo

### Request Body
- **bucketAntiDDosInfo** (object) - Required - Bucket DDoS protection configuration.
  - **status** (string) - Required - The status of DDoS protection. Possible values: `Enabled`, `Disabled`.

### Response
#### Success Response (200)
- **No content**

## ListBucketAntiDDosInfo

### Description
Retrieves a list of DDoS protection information for OSS buckets.

### Method
GET

### Endpoint
/bucket?antiDDosInfo

### Response
#### Success Response (200)
- **bucketAntiDDosInfos** (array)
  - **bucketAntiDDosInfo** (object)
    - **bucketName** (string) - The name of the bucket.
    - **status** (string) - The status of DDoS protection. Possible values: `Enabled`, `Disabled`.
```

--------------------------------

### POST /?metaQuery&comp=delete

Source: https://help.aliyun.com/zh/oss/developer-reference/closemetaquery

Closes the metadata management function for a Bucket. This is an asynchronous operation that deletes the metadata index.

```APIDOC
## POST /?metaQuery&comp=delete

### Description
Calls the CloseMetaQuery interface to close the metadata management function of a storage space (Bucket). After closing, OSS automatically deletes the metadata index library, and metadata indexing will no longer be available.

### Method
POST

### Endpoint
/?metaQuery&comp=delete

### Request Example
POST /?metaQuery&comp=delete HTTP/1.1
Host: oss-example.oss-cn-hangzhou.aliyuncs.com
Date: Thu, 17 Apr 2025 13:08:38 GMT
Authorization: OSS4-HMAC-SHA256 Credential=LTAI********************/20250417/cn-hangzhou/oss/aliyun_v4_request,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e

### Response
#### Success Response (200)
- **x-oss-request-id** (string) - The unique ID for the request.
- **x-oss-server-time** (string) - The time taken by the server to process the request.

#### Response Example
HTTP/1.1 200 OK
x-oss-request-id: 5C1B138A109F4E405B2D****
Date: Mon, 26 Jul 2021 13:08:38 GMT
Content-Length: 0
Connection: keep-alive
Server: AliyunOSS
x-oss-server-time: 178
```

--------------------------------

### DeleteBucketDataAccelerator

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Deletes the OSS Accelerator configuration for a bucket.

```APIDOC
## DELETE /?dataAccelerator

### Description
Delete the OSS Accelerator.

### Method
DELETE

### Endpoint
/?dataAccelerator
```

--------------------------------

### DeleteObject

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Deletes a single Object from the OSS bucket.

```APIDOC
## DELETE DeleteObject

### Description
Deletes a single Object from the OSS bucket.

### Method
DELETE

### Endpoint
/ObjectName
```

--------------------------------

### Bucket CORS Operations

Source: https://help.aliyun.com/zh/oss/developer-reference/api-reference

APIs for managing Cross-Origin Resource Sharing (CORS) rules for Buckets.

```APIDOC
## PutBucketCors

### Description
Sets Cross-Origin Resource Sharing (CORS) rules for a Bucket.

### Method
POST

### Endpoint
/PutBucketCors

## GetBucketCors

### Description
Retrieves the current Cross-Origin Resource Sharing (CORS) rules for a Bucket.

### Method
GET

### Endpoint
/GetBucketCors

## DeleteBucketCors

### Description
Disables Cross-Origin Resource Sharing (CORS) for a Bucket and clears all rules.

### Method
POST

### Endpoint
/DeleteBucketCors

## Options

### Description
Handles preflight OPTIONS requests for CORS, allowing browsers to determine if the actual request should be sent.

### Method
OPTIONS

### Endpoint
/* (Applies to all bucket resources for CORS preflight checks)
```

--------------------------------

### POST /ObjectName?append&position=Position

Source: https://help.aliyun.com/zh/oss/developer-reference/appendobject

Uploads data to an object in an appendable manner. The first append operation must have a position of 0, and subsequent operations must use the position returned by the previous response.

```APIDOC
## POST /ObjectName?append&position=Position

### Description
Uploads data to an object in an appendable manner. The first append operation must have a position of 0, and subsequent operations must use the position returned by the previous response.

### Method
POST

### Endpoint
/{ObjectName}?append&position={Position}

### Parameters
#### Path Parameters
- **ObjectName** (string) - Required - The name of the object to append to.

#### Query Parameters
- **append** (string) - Required - Specifies the AppendObject operation.
- **position** (string) - Required - Specifies the starting position for the append operation.

#### Request Headers
- **Cache-control** (string) - Optional - Specifies the web cache behavior.
- **Content-Disposition** (string) - Optional - Specifies the download filename.
- **Content-MD5** (string) - Optional - MD5 hash for integrity check.
- **Expires** (string) - Optional - Expiration time.
- **x-oss-server-side-encryption** (string) - Optional - Server-side encryption method (AES256, KMS, SM4).
- **x-oss-object-acl** (string) - Optional - Access control list for the object.
- **x-oss-storage-class** (string) - Optional - Storage class (Standard, IA, Archive).
- **x-oss-meta-*** (string) - Optional - User-defined metadata.
- **x-oss-tagging** (string) - Optional - Object tagging information.

### Response
#### Success Response (200)
- **x-oss-next-append-position** (64-bit integer) - The position for the next append operation.
- **x-oss-hash-crc64ecma** (64-bit integer) - The 64-bit CRC value of the object.
```

--------------------------------

### PutBucketArchiveDirectRead

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Enables or disables the Archive Direct Read feature for a specified bucket.

```APIDOC
## PUT /?archiveDirectRead

### Description
Enable or disable the Archive Direct Read feature for the bucket.

### Method
PUT

### Endpoint
/?archiveDirectRead
```

--------------------------------

### GetBucketArchiveDirectRead

Source: https://help.aliyun.com/zh/oss/developer-reference/list-of-operations-by-function

Retrieves the current status of the Archive Direct Read feature for a bucket.

```APIDOC
## GET /?archiveDirectRead

### Description
Check whether the Archive Direct Read feature is enabled for the bucket.

### Method
GET

### Endpoint
/?archiveDirectRead
```

--------------------------------

### Error Codes for AppendObject

Source: https://help.aliyun.com/zh/oss/developer-reference/appendobject

Lists and describes the error codes that can be returned by the AppendObject API.

```APIDOC
## Error Codes

- **ObjectNotAppendable** (409): Attempting to perform an AppendObject operation on a non-Appendable Object.
- **PositionNotEqualToLength** (409):
  - The `position` value does not match the current Object's length. You can obtain the next position from the `x-oss-next-append-position` response header and retry the request. Due to concurrency, even if the `position` is set to `x-oss-next-append-position`, the request might still fail with this error code.
  - If the `position` value is 0, the request succeeds if there is no Appendable Object with the same name or if the length of the existing Appendable Object is 0. Otherwise, it is considered a mismatch between `position` and Object length, and this error code is returned.
- **InvalidArgument** (400): Invalid values set for parameters like `x-oss-storage-class` or `x-oss-object-acl`.
- **FileImmutable** (409): Returned when attempting to delete or modify data within a protected bucket.
- **KmsServiceNotEnabled** (403): The Key Management Service (KMS) has not been enabled in the console when using KMS encryption.
```

--------------------------------

### Abort Multipart Upload

Source: https://help.aliyun.com/zh/oss/developer-reference/abortmultipartupload

Cancels a multipart upload event and deletes the corresponding part data. It's recommended to either complete or cancel multipart uploads promptly to avoid unnecessary storage charges.

```APIDOC
## DELETE /ObjectName?uploadId=UploadId

### Description
Cancels a multipart upload event and deletes the corresponding part data. It's recommended to either complete or cancel multipart uploads promptly to avoid unnecessary storage charges.

### Method
DELETE

### Endpoint
`/{ObjectName}?uploadId={UploadId}`

### Parameters
#### Query Parameters
- **uploadId** (string) - Required - The unique identifier for this multipart upload event.

### Request Example
```
DELETE /multipart.data?uploadId=0004B9895DBBB6E**** HTTP/1.1
Host: oss-example.oss-cn-hangzhou.aliyuncs.com
Date: Wed, 22 Feb 2012 08:32:21 GMT
Authorization: OSS4-HMAC-SHA256 Credential=LTAI********************/20250417/cn-hangzhou/oss/aliyun_v4_request,Signature=a7c3554c729d71929e0b84489addee6b2e8d5cb48595adfc51868c299c0c218e
```

### Response
#### Success Response (204)
- **Content-length** (integer) - The length of the response body.
- **x-oss-request-id** (string) - The request ID.
- **Date** (string) - The date and time the request was processed.

#### Response Example
```
HTTP/1.1 204 
Server: AliyunOSS
Content-length: 0
Connection: keep-alive
x-oss-request-id: 059a22ba-6ba9-daed-5f3a-e48027df****
Date: Wed, 22 Feb 2012 08:32:21 GMT
x-oss-server-time: 86
```

### Error Handling
- **NoSuchUpload** (404) - Indicates that the specified uploadId does not exist.
```

=== COMPLETE CONTENT === This response contains all available snippets from this library. No additional content exists. Do not make further requests.